# CLAUDE.md — Legal Lens Master Project Definition

> **Status:** PLANNING PHASE. No implementation has begun. This document is the single source of truth for what Legal Lens is, why it exists, and how it should eventually be built. It is a living document — update it after every planning decision.
>
> **Tagging convention used throughout this document:**
> - `OPEN QUESTION` — something we haven't discussed yet; needs a conversation.
> - `DECISION REQUIRED` — a fork in the road has been identified; the project cannot proceed past a certain point without picking a side.
> - `ASSUMPTION TO VALIDATE` — a working assumption is in place (often to keep this document coherent) but has NOT been confirmed by the user and may be wrong.
>
> Do not treat anything tagged above as settled. Do not write code against an `OPEN QUESTION` or unvalidated `ASSUMPTION`.

---

## 1. Project Vision

Legal Lens is an AI-assisted platform that helps a person who is facing a legal situation — and who does not necessarily know any legal terminology — move from *"something happened to me and I don't know what to do"* to a structured, evidence-linked, source-cited understanding of their situation: what facts they have, what legal provisions might apply, what's missing, what the other side might argue, and what documents or next steps could help.

Legal Lens is explicitly **not** trying to be a lawyer, replace legal advice, or predict court outcomes. It is trying to be the most rigorous, traceable, well-organized **case preparation and legal orientation assistant** possible, built so that every claim it makes can be traced to a verifiable source, and every place it is uncertain is presented as uncertain.

## 2. Problem Statement

People encountering a legal problem (a dispute, a potential offence, a contract issue, a rights violation, etc.) typically face:

- Not knowing which laws or legal categories apply to their situation.
- Not knowing what facts/evidence matter and what to collect.
- Not being able to afford or access a lawyer for early-stage orientation.
- Not knowing how to structure a complaint, notice, or case file.
- Not knowing the weaknesses in their own case, or what the other side might argue.
- Difficulty finding relevant statutory provisions or precedents in dense legal text.

Legal Lens addresses the **orientation and preparation** gap — not the advocacy or adjudication gap.

`ASSUMPTION TO VALIDATE`: The core problem being solved is *pre-lawyer orientation and case preparation*, not *replacing* a lawyer at any stage. This acts like an assistant to lawyers/paralegals/normal humans. This shapes almost every downstream design decision (disclaimers, automation limits, human-in-the-loop requirements). Confirm this framing with the user.

## 3. Target Users

**DECISION (2026-08-21):** v1 targets multiple personas from day one:

- **Persona A — Ordinary citizen / self-represented individual.** No legal knowledge. Needs plain-language interview, heavy hand-holding, strong disclaimers, cannot be expected to validate legal citations themselves.
- **Persona B — Law student.** Learning-oriented. Wants to see reasoning, citations, counterarguments, and the "why" behind classifications. More tolerant of imperfect UX, more capable of catching errors.
- **Persona C — Paralegal / junior associate / lawyer's assistant.** Uses the tool to accelerate case-file preparation, evidence organization, and first-draft documents under supervision of a licensed lawyer. Highest tolerance for structured/technical UI, lowest tolerance for hallucination since output feeds real practice.
- **Persona D — Practicing lawyer (power user).** Wants speed: fast retrieval, precedent search, draft generation, case-strength summarization for cases they already understand.

`DECISION REQUIRED`: Multi-persona v1 means the same underlying case data and outputs must be presentable at different levels of plain-language vs. technical density. This needs a UI/content-layer decision: a single adaptive presentation, or persona-specific views over the same Case Builder state. Not yet designed.

`ASSUMPTION TO VALIDATE`: Given this is a **portfolio project** (see §3.1 below), true simultaneous multi-persona support across the full feature set is a large surface area. Recommend the *architecture* support multiple personas (data model, agent outputs are persona-agnostic and structured), while the first working vertical slice demonstrates one persona's full flow end-to-end before building out the others — otherwise nothing reaches "done." This needs explicit confirmation from the user before Phase 4 begins.

### 3.1 Project Context

**DECISION (2026-08-21):** This is a **portfolio project** — built to demonstrate depth of thinking, system design quality, and technical execution, not to serve real production users or real legal matters. This has significant implications, flagged here so they aren't silently lost:

- Real users' real legal situations should **not** be processed by this system unless/until privacy, security, and legal-risk work (§18, §5) is substantially further along than a portfolio timeline typically allows. Demo/synthetic case data should be the default assumption for development and demonstration.
- Compliance rigor (data protection law, unauthorized-practice-of-law rules) matters for *demonstrating awareness* of these constraints in the design, but does not need to be operationalized to production-grade certification for a portfolio artifact.
- Scale/availability targets (§7) can stay modest — this is not being built for concurrent real traffic.
- **Scope risk flagged explicitly:** the combination chosen — multi-persona, broad multi-domain, India + state laws — is a large scope for a project not backed by a team or production timeline. See §12.1 for the corresponding recommendation to sequence breadth *after* a working narrow slice, and `COMPLETION.md`'s Open Items list for the tracked risk.

## 4. Core Use Cases

1. A user describes a situation in plain language; the system conducts a dynamic interview to fill gaps.
2. The system builds a structured Case file: facts, parties, timeline, claims, evidence, applicable law.
3. The system retrieves and cites relevant statutory provisions with source traceability.
4. The system identifies evidence gaps and asks the user to address them.
5. The system generates counterarguments / weaknesses ("devil's advocate" pass).
6. The system helps prepare draft documents (complaint, notice, chronology, evidence list) marked clearly as AI-assisted drafts requiring professional review.
7. The system helps prepare for likely questioning (from opposing side, court, or investigators) strictly from known facts.
8. The system presents case strength as a structured, multi-dimensional assessment (not a single win-probability number) — coverage, uncertainty, contradictions, counterarguments.
9. (Optional/late-phase) The system surfaces similar precedents/case law with explicit reliability caveats.

## 5. Product Boundaries — What Legal Lens Is NOT

These boundaries are foundational, not incidental, given the legal-risk profile of this product:

- **Not a substitute for a licensed lawyer.** It does not give "legal advice" in the professional-responsibility sense; it gives structured legal *information* and *organization*.
- **Not a guarantee of outcome.** No win-probability numbers without a rigorously defensible methodology (none currently exists for this domain at this fidelity — see §16).
- **Not an autonomous filer.** It does not submit documents to courts/authorities on a user's behalf (at least not in any near-term version).
- **Not a source of law itself.** Every legal claim must cite a retrieved, verifiable source — the model's own "knowledge" is never treated as authoritative for statutory content.

`DECISION REQUIRED`: Jurisdictional practice-of-law regulations (e.g., unauthorized practice of law rules) may constrain what the product can legally do/say in a given market. This needs explicit legal review before any public launch — flagged here as a hard product-boundary risk, not just a UX concern.

## 6. Functional Requirements Overview

Grouped by the 7 core functionalities from the original brief. Each is expanded in its own section later in this document.

| # | Functionality | Section |
|---|---|---|
| 1 | Dynamic Legal Interview System | §8 |
| 2 | Case Builder | §9 |
| 3 | Evidence Management & Gap Analysis | §10 |
| 4 | Legal Knowledge & RAG System | §11–12 |
| 5 | Previous Case / Precedent Reference | §13 |
| 6 | Question & Answer Preparation | §14 |
| 7 | Case Strength Analysis | §15 |

Plus supporting system: Multi-agent architecture (§7), Document Generation (see §9.6), Explainability/Hallucination Prevention (§16), Jurisdiction & Temporal Validity (§17), Privacy/Security (§18), Architecture & Tech (§19).

## 7. Non-Functional Requirements

- **Traceability:** Every legal-provision claim must be linkable to an ingested source document + version + effective-date window.
- **Explainability:** Every classification, recommendation, or generated argument must be inspectable — "why did the system say this."
- **Safety over completeness:** The system must prefer saying "uncertain / insufficient information" over fabricating a confident-sounding but unsupported answer.
- **Auditability:** All interview exchanges, retrieved sources, and generated outputs relevant to a case must be logged for later review (by the user, and potentially a supervising lawyer).
- **Privacy:** Legal situations are highly sensitive (domestic violence, criminal allegations, financial disputes). Data handling must assume worst-case sensitivity by default.
- **Availability / latency:** `OPEN QUESTION` — no target SLAs defined yet; depends on whether this is a live product or a research/portfolio project (see §21 discovery topic).
- **Correctness over speed:** Multi-agent pipelines (interview, retrieval, drafting) should be allowed to take longer if it materially improves citation accuracy.

## 8. Multi-Agent Architecture

`ASSUMPTION TO VALIDATE`: The system uses an **orchestrator + specialist agents** pattern (a central controller sequences/coordinates the specialist agents below) rather than fully autonomous peer-to-peer agent communication. This is the safer, more debuggable default for a legal-risk product, but must be confirmed with the user, who may prefer a different topology (e.g., a simpler single-agent-with-tools design for MVP, deferring "multi-agent" complexity).

### 8.1 Orchestrator (implicit in the brief, not named as an agent but required)

- Owns the Case Builder state.
- Decides which specialist agent(s) to invoke at each step of the interview/case-building process.
- Resolves conflicting outputs between agents (e.g., Legal Classification suggests category A, Devil's Advocate flags contradiction).
- Enforces the "never present unsupported legal claims" invariant across all agent outputs before they reach the user.

**DECISION (2026-08-24):** Hand-coded state machine, not an LLM-driven planner — the safer, more testable default for a legal-risk product, as originally recommended.

### 8.2 Fact Extraction Agent

- **Input:** Raw user narrative (text, possibly multi-turn), interview answers.
- **Output:** Structured entities (people, orgs, locations, dates, events, actions, relationships) + explicit tagging of each statement as `fact` / `assumption` / `opinion` / `allegation` / `unknown`. Timeline draft.
- **Failure modes to design against:** merging distinct people/events, mis-tagging opinion as fact, silently dropping ambiguous statements instead of flagging them.

### 8.3 Legal Classification Agent

- **Input:** Structured facts from Fact Extraction, jurisdiction context.
- **Output:** Ranked list of *possible* legal categories/offences/disputes/remedies — explicitly multiple hypotheses, not a single verdict. Confidence/rationale per hypothesis.
- **Failure modes:** premature narrowing to one legal theory; ignoring plausible alternative characterizations; jurisdiction mismatch.

### 8.4 Law Retrieval Agent

- **Input:** Legal classification hypotheses + facts.
- **Output:** Retrieved statutory provisions/sections/rules with source metadata (document, version, effective date, section number) — see RAG architecture §12.
- **Hard requirement:** No provision is surfaced without a retrieval hit against the verified knowledge base. The agent must not "recall" a section number from model parametric knowledge.

### 8.5 Legal Strategy Agent

- **Input:** Facts, legal classifications, retrieved provisions, evidence status.
- **Output:** **DECISION (2026-08-21):** Ranks possible approaches/remedies/procedural options and surfaces a suggested best path — going beyond a flat options-list, while the suggestion must remain explicitly non-binding (framed as "based on currently known facts, X appears strongest, because..." rather than "you should do X").
- **Product-boundary consequence:** Because this agent actively ranks and recommends, it carries meaningfully more legal-risk/product-boundary exposure than a pure options-lister (see §5). This raises the bar on:
  - Disclaimer strength and placement (must be adjacent to every ranked recommendation, not just a global footer).
  - Traceability: the ranking rationale must cite the specific facts/evidence/provisions driving it (§16) so the "why" is inspectable, not a black-box preference.
  - `DECISION REQUIRED`: Should ranked recommendations be gated behind an explicit user acknowledgment ("this is not legal advice, do you want to see a suggested path") before being shown? Recommend yes; not yet confirmed.

### 8.6 Devil's Advocate / Counterargument Agent

- **Input:** Full case state (facts, evidence, claims).
- **Output:** Weaknesses, missing-evidence-driven vulnerabilities, plausible opposing arguments, alternative interpretations of the same facts.
- **Design constraint:** Must operate on the same fact/evidence base as other agents — cannot invent facts to attack, only expose weaknesses in what exists or is missing.

### 8.7 Complaint / Case Document Generation Agent

- **Input:** Verified case state + retrieved legal provisions.
- **Output:** Structured drafts (complaint, legal notice, case summary, chronology, evidence list, statement, question sets).
- **Hard requirement:** Does not fabricate facts not present in the Case Builder; does not invent citations.

**DECISION (2026-08-24):** The watermarking/labeling requirement above ("every generated document must be watermarked/labeled as an AI-assisted draft requiring professional review before use") is **removed at the user's explicit instruction**. This was previously implemented two ways — a required first line in generated content, and reliance on `DocumentDraft.review_status` defaulting to `"ai_draft"` — and both the content-level label and the enforcement instruction have been removed from the agent's system prompt (`review_status` itself still exists as a DB field, unaffected). This directly narrows §5's product-boundary posture and §22's human-in-the-loop principle ("before any generated document is treated as final, always requires explicit user acknowledgment that it's an AI draft") — no watermark now appears in generated documents by default. Flagged here rather than left implicit: this is a live legal-risk/product-boundary gap for any deployment beyond this portfolio project's own development use, not something the codebase mitigates elsewhere.

### 8.8 Agent Communication & Orchestration

**DECISION (2026-08-24):**
- Agents communicate via **shared structured Case Builder state**, not message-passing/conversation — auditable, avoids uncontrolled inter-agent chat.
- Agent invocation is **synchronous per user turn** for v1 (no background/async agent runs yet — e.g., Evidence Gap Analysis re-runs when triggered by the orchestrator after a relevant state change, not on an independent background loop).
- **DECISION (2026-08-24):** "Surface, don't silently resolve." No agent has authority over another. `Orchestrator._flag_agent_conflicts()` (run inside `case_strength()`) detects when Legal Classification's hypotheses coexist with a "strong"-severity Devil's Advocate weakness and surfaces both explicitly as an unresolved tension — the exact scenario this open question named — rather than the orchestrator picking one agent's view as authoritative. This is deliberately a simple juxtaposition rule, not a reconciliation algorithm: there's no principled basis for the orchestrator to decide which agent is "more right."

## 9. Dynamic Legal Interview System

### 9.1 Design Goals
- Ask only legally material questions — no interrogation for its own sake.
- Adapt based on: problem type, facts already known, jurisdiction, current legal-classification hypotheses, missing evidence, and detected contradictions.
- Maintain a structured, appendable record of every answer (feeds directly into Case Builder).

### 9.2 Question Selection Logic (conceptual, not implementation)
For each turn:
1. Compute current "information state" (what's known/unknown/contradictory) from Case Builder.
2. Ask Legal Classification Agent: given current facts, what additional facts would most change or confirm the hypothesis set?
3. Rank candidate questions by expected information gain / legal materiality.
4. Select next question(s); avoid re-asking already-answered material.
5. Determine sufficiency: stop interviewing a topic when marginal value of more questions is low, OR user signals they don't know / can't provide more.

**DECISION (2026-08-24):** Whole-interview stopping criterion resolved as a hybrid: a fixed hard question budget (implemented as `INTERVIEW_QUESTION_BUDGET = 10`, checked before any agent call — no LLM self-regulation for the hard stop), the agent's own per-turn "sufficient" signal as a soft stop, and the user can always end early. No confidence-threshold-on-classification mechanism implemented (would need a calibrated confidence score, not something the current Legal Classification Agent output supports).

### 9.3 Contradiction Handling
- If a new answer contradicts an earlier one, the system must flag it explicitly (not silently overwrite), tag both statements, and may ask a clarifying question.
- Contradictions become part of the Case Builder's "disputed facts" set (see §10) and are visible to the Devil's Advocate Agent.

**Implementation note (2026-08-24):** built as a dedicated agent call comparing each new interview answer against every prior statement/answer. Flagged via `InterviewTurn.contradicts_ref` — a deliberate non-FK polymorphic string reference (can point at either a `Statement.id` or another `InterviewTurn.id`), not a strict foreign key, since both are valid contradiction targets. "Clarifying question" follow-up (the "may ask" clause above) is not automated — a contradiction is surfaced to the caller, not auto-resolved into a new interview question.

### 9.4 Jurisdiction Detection
**DECISION (2026-08-24):** Both. `POST /cases` accepts an optional explicit `jurisdiction` override (default "India - Karnataka" unchanged). Separately, `Orchestrator._detect_jurisdiction_mismatch()` scans each submitted narrative for keywords strongly associated with a different Indian state and returns a non-binding `jurisdiction_warning` when found. This is a deliberately crude keyword heuristic — not NER, not a geocoder — false positives are expected and acceptable because it only ever produces a warning, never an automatic jurisdiction change. Cross-jurisdiction facts (the second half of the original open question) are not specially handled beyond this warning — no logic exists for a case spanning two jurisdictions' law simultaneously.

## 10. Case Builder

### 10.1 Purpose
The single structured, evolving representation of the user's legal situation. All agents read from and write to it (via the orchestrator). It is the artifact that gets exported into documents, shown to the user, and (eventually) reviewed by a human lawyer.

### 10.2 Conceptual Data Model (sketch — not a DB schema; schema work is implementation phase)

- **Case** — id, status, jurisdiction, created/updated timestamps, summary.
- **Party** — id, role (user/opposing/witness/third-party/authority), name or identifier, relationship-to-user.
- **Relationship** — party ↔ party, type, description.
- **Event** — id, timestamp (or estimated/approximate), description, participants, source (user statement vs. evidence-derived), confidence.
- **Timeline** — ordered/orderable sequence of Events, with explicit handling of uncertain/approximate dates.
- **Statement** — raw text, classification (`fact`/`assumption`/`opinion`/`allegation`/`unknown`), linked Event/Party if applicable, source (interview turn reference).
- **Claim/Allegation** — id, description, asserted-by (party), supporting Statements, supporting Evidence, status (supported/unsupported/disputed).
- **Evidence** — see §10.3 (Evidence model) below; linked to Claims and Events.
- **LegalProvision (reference)** — pointer into the RAG knowledge base (not stored duplicated), plus the reasoning link explaining *why* it was retrieved for this case.
- **LegalAction/Remedy (candidate)** — id, description, supporting provisions, procedural notes, status (candidate/discussed/rejected).
- **Counterargument** — id, description, target Claim(s), source (Devil's Advocate Agent), status.
- **OpenQuestion (case-level)** — things the system still needs clarified — distinct from the document-level `OPEN QUESTION` tags in this file; this is per-case runtime state.
- **DocumentDraft** — id, type (complaint/notice/summary/etc.), content, generation timestamp, source Case Builder version, review-status (AI-draft / lawyer-reviewed / finalized).

`ASSUMPTION TO VALIDATE`: Case Builder state should be **versioned** (every material change creates a new version, not an overwrite) so that "what did the system know when it generated this document" is always reconstructable. This is important for auditability but adds real implementation complexity — confirm this is wanted for MVP or deferred.

## 11. Evidence Model & Evidence Gap Analysis

### 11.1 Evidence Types (from brief)
Documents, contracts/agreements, messages, emails, images, videos, audio, witness information, financial records, other digital evidence, government documents, other.

### 11.2 Core Distinction (must be enforced everywhere in the system)
1. **Facts claimed by the user** (unverified narrative statements).
2. **Evidence provided by the user** (actual artifacts — documents, media, records).
3. **Facts independently supported by evidence** (a claim that has at least one linked, plausible piece of evidence).

These three must never be conflated in the UI, in generated documents, or in internal agent reasoning. A claim with no linked evidence stays visibly "unsupported," even if plausible.

### 11.3 Evidence Gap Analysis
For each Claim/Allegation in the Case Builder:
- Does it have linked Evidence? (yes/no/partial)
- Is the linked Evidence itself disputed or of uncertain provenance?
- What evidence *type* would typically support this kind of claim (domain-informed heuristic — e.g., a contract dispute typically benefits from the written contract, correspondence, payment records)?
- Surface this as a structured gap list, not a hidden internal score.

**DECISION (2026-08-21):** v1 includes **basic content extraction (OCR/text extraction)** for text-bearing evidence (scanned documents, images of text, PDFs, etc.) — beyond pure metadata logging, but stopping short of full media (audio/video) content analysis. Consequences to design for:
- Extracted text becomes a candidate source for fact-verification (e.g., checking if a claimed contract term is actually present in the uploaded contract) — but extracted text must be treated as *evidence content*, distinct from user-asserted facts (§11.2's three-way distinction still applies: extracted text supports evidence-backed facts, it does not become a "fact" on its own without being linked to a claim).
- OCR errors are a new failure mode: extracted text must carry a confidence/quality indicator, and low-confidence extractions should not silently feed downstream reasoning as if reliable.
- Privacy/security surface area increases (document content is processed, not just stored) — reinforces the need for encryption-at-rest and access control (§18) before any real documents are handled.
- Audio/video evidence in v1 is logged with metadata + user description only; no transcription/analysis. `OPEN QUESTION`: is basic audio transcription (not full analysis) worth adding later given how common WhatsApp/call-recording evidence is in real disputes? Parked for future expansion (§23).

**IMPLEMENTED (2026-08-25):** §11.3's "is the linked evidence itself disputed or of uncertain provenance?" question now has a real answer: `Evidence.disputed` (boolean, never auto-inferred — only set via an explicit `PATCH /cases/{id}/evidence/{evidence_id}/dispute` call) plus `extraction_confidence` together drive all four `ClaimStatus` values in `Orchestrator._compute_claim_statuses()`: disputed evidence → `disputed`; undisputed evidence with low/no-confidence extracted text → `partially_supported`; otherwise → `supported`/`unsupported` as before. Previously only `supported`/`unsupported` were computed, with the other two states left unimplemented pending this exact modeling work.

## 12. Legal Knowledge Base & RAG Architecture

### 12.1 Scope Decision

**DECISION (2026-08-21):** Jurisdiction is **India, including select state laws** (not central-law-only); domain scope is **broad multi-domain from the start** (not a single narrow slice). This is a deliberate scope expansion beyond the original recommendation, made knowingly as part of a portfolio project's ambition (see §3.1).

`DECISION REQUIRED` (still open, now narrower): "Broad multi-domain" and "select state laws" still need concrete boundaries before ingestion work can start:
- Which domains, specifically, are "in" for the first working version vs. the full target set? (e.g., is criminal law in scope alongside civil/consumer/contract from the very first working slice, or added in a second wave?)
- Which states' laws, specifically, are "select" — and on what basis are they selected (most common disputes, most-populous states, arbitrary)?

**DECISION (2026-08-24):** Sequencing confirmed by user. "Broad multi-domain, India + state laws" remains the **long-term target architecture** (data model, agent outputs, and RAG pipeline are built domain-agnostic from the start), but Phase 3 (`COMPLETION.md`) ingests one domain first, end-to-end — proving out ingestion pipeline, chunking, citation-binding, and evaluation — before expanding domain-by-domain. This avoids a shallow, low-quality pass across everything at once, which would undermine the traceability/accuracy requirements that are this project's core differentiator.

**DECISION (2026-08-24):** First-wave domain is **criminal law** — Bharatiya Nyaya Sanhita (BNS) 2023 + Bharatiya Nagarik Suraksha Sanhita (BNSS) 2023 + Bharatiya Sakshya Adhiniyam (BSA) 2023, plus their historical predecessors (IPC, CrPC, Evidence Act) for pre-July-2024 events per §12.3's temporal-validity requirement. This is a deliberately higher-risk/higher-payoff choice than the originally recommended consumer-protection starting point: it directly exercises §12.3's dual-code (old law/new law) modeling problem, but also raises sensitivity earlier (§18's mandatory-reporting/minor-safety policy gap becomes relevant sooner — flagged for Phase 1's practice-of-law/privacy research tasks below). `DECISION REQUIRED` (Phase 1, remaining): domain-by-domain expansion order after criminal law.

**DECISION (2026-08-24), Tiers 4–5 descoped 2026-08-24:** The criminal-law v1 document scope is a **three-tier** hierarchy (originally proposed as five; Tiers 4 and 5 were researched and then explicitly descoped once research showed no viable source):

1. **Core legislation** — BNS (substantive), BNSS (procedural), BSA (evidence), plus historical IPC/CrPC/Evidence Act for pre-July-2024 events (§12.3).
2. **Central special laws** — POCSO, NDPS Act, UAPA, IT Act (the criminal-law-relevant provisions).
3. **State-specific laws** — Karnataka Police Act 1963, Karnataka Control of Organised Crimes Act 2000 (KCOCA), Karnataka Prevention of Dangerous Activities Act 1985 ("Goonda Act"), Karnataka Police Complaints Authority rules (§12.5).

**Descoped — Judicial precedents:** researched and rejected 2026-08-24. No true citator (overruled/distinguished/followed tracking) exists at portfolio-project cost — Indian Kanoon's API exposes only raw cite/cited-by counts; real citator products are SCC Online/Manupatra-subscription-tier only. Rather than ship an unreliable version of §13.2's status-tracking requirement, precedent retrieval stays deferred per §13.2's original 2026-08-21 decision.

**Descoped — Execution & police manuals:** researched and rejected 2026-08-24. No public downloadable source found for a Karnataka Police Manual or consolidated Standing Orders — the historical 1965/1973 manual is archival/physical only; ksp.karnataka.gov.in hosts circulars and an RTI-mandated disclosure manual, but not an operational manual. Further pursuit (a full department-wise DPAL index crawl, or an RTI request) was judged out of scope for a portfolio project.

### 12.2 Source Authority & Hierarchy

**DECISION (2026-08-24):** Source authority hierarchy for the criminal-law slice, based on Phase 1 research:

1. **e-Gazette** (egazette.gov.in) — ground truth for enacted text and effective dates; used to validate the other sources, not as the primary day-to-day working source (PDF-only, not structured by section).
2. **India Code** (indiacode.gov.in) — primary working source for tiers 1–2 (core legislation + central special laws); official Ministry of Law and Justice repository, structured by section, retains historical/repealed acts.
3. **Karnataka DPAL** (dpal.karnataka.gov.in) — primary source for tier 3 (Karnataka acts).
4. **Indian Kanoon**, via its **paid API** (not scraping — their terms discourage raw scraping) — primary source for tier 4 (judicial precedents), and secondary cross-check for tiers 1–3 when India Code text is ambiguous.
5. SCC Online / Manupatra — excluded; subscription cost not viable for a portfolio project.

**Acquisition method (DECIDED 2026-08-24, REVERSED 2026-08-24):** originally decided as manual human download to sidestep the unverified robots.txt/ToS status of India Code and Karnataka DPAL. **User asked the assistant to download the documents instead.** Before doing so, robots.txt was actually checked rather than assumed either way: Karnataka DPAL explicitly permits all crawling; India Code's robots.txt was unreachable/inconsistent across its domain variants, but every document was fetched as a single direct, publicly-known document URL (not systematic/bulk crawling of the site), a materially different risk profile than what the original manual-download decision was declining. All 13 available documents (of 14 planned — one, the Karnataka Police Complaints Authority "rules," turned out not to be a separate document at all; see `MANIFEST.md`) were downloaded, and each was **verified to contain real extractable text, not a scanned image** — one initial IPC download (56MB, scanned) failed this check and was redone from a different source before being kept. Full per-document provenance (source URL used, and where it diverged from the originally-planned source) is recorded in `MANIFEST.md`, including two documents (KCOCA, Goonda Act) sourced from a secondary aggregator (PRS Legislative Research) rather than an official government source, flagged for re-verification. All were ingested (5,091 chunks); Law Retrieval now returns real hits — verified live against a query for "punishment for murder" correctly returning BNS §103.

- How is source provenance stored (publisher, retrieval date, document version/edition, URL/citation, checksum)? — `OPEN QUESTION`, still needs a concrete schema (Phase 2 data-model work).
- What is the process when two sources disagree on text? — resolved by the hierarchy above (higher-ranked source wins); e-Gazette is the tiebreaker of last resort.

### 12.3 Historical / Repealed Law Handling
India recently replaced IPC/CrPC/Evidence Act with BNS/BNSS/BSA (effective July 1, 2024). This is a first-class design problem, not an edge case:
- Events that occurred **before** the transition date are generally governed by the old law (IPC/CrPC/Evidence Act) for many purposes; events after are governed by the new codes. `DECISION REQUIRED` on exactly how transitional/saving-clause rules are modeled — this is legally nuanced and should not be hardcoded as a naive date cutoff without review.
- The knowledge base must store **both** old and new provisions, each tagged with effective-date ranges, and the Law Retrieval Agent must select based on the **event date**, not the query date.
**DECISION (2026-08-24):** Confirmed as the data-model requirement (no longer just an assumption). Every ingested provision — BNS/BNSS/BSA and historical IPC/CrPC/Evidence Act alike — carries explicit `effective_from` / `effective_to` (or `repealed_by`) fields, plus a pointer to its successor/predecessor provision where applicable (e.g., BNS §103 ↔ IPC §302). The Law Retrieval Agent selects based on the event date, not the query date, per this schema. Schema work itself (field types, storage) is Phase 2.

**IMPLEMENTED (2026-08-25):** `backend/app/ingestion.py`'s `ACT_EFFECTIVE_DATES` table now populates `effective_from`/`effective_to`/`repealed_by`/`successor_of` on every ingested chunk's metadata, keyed by act_name. Dates are best-effort from public enactment/commencement knowledge, not independently re-verified against e-Gazette per document — consistent with §12.2's acquisition policy. **Verified live**: a "punishment for murder" query returns BNS §103 tagged `effective_from: 2024-07-01, successor_of: IPC_1860`; a "criminal intimidation" query returns both BNS §351 and its historical IPC §503/506/507 counterparts, correctly tagged `effective_to: 2024-06-30, repealed_by: BNS_2023`. The Law Retrieval Agent's own selection-by-event-date logic (as opposed to just carrying the metadata) is still not built — this closes the *data* half of this decision, not yet the *retrieval-time filtering* half.

### 12.4 Amendment Tracking
**DECISION (2026-08-24):** **Automated periodic re-check** — a scheduled job diffs source sites (India Code, e-Gazette, Karnataka DPAL) against the ingested corpus and flags/applies detected changes automatically. This overrides the earlier recommendation of manual-review-gated updates, which was made specifically because of this product's legal-risk profile (§7's "safety over completeness," §16's hallucination-prevention requirements). **Risk flagged, not resolved by this decision:** silent automated application of a detected "amendment" could ingest a premature, misread, or incorrectly-diffed change without human verification — directly in tension with §7's traceability/explainability requirements. **RESOLVED 2026-08-24 in the direction of the middle ground flagged above**, though only half-built: `ingest_all()` now does automated local *change-detection* (SHA-256 checksum per source file, re-embeds only new/changed files, verified live against the real 13-document corpus — a second run correctly skipped all 13 unchanged files). What's still missing is the actual "diffs source sites" half — there is no fetch-and-compare against India Code/e-Gazette/Karnataka DPAL, so this only detects changes to files already sitting in `data/raw/criminal-law/`, not changes at the authoritative source. The risk flagged above (silent application of a misread amendment) is narrowed but not eliminated: applying a changed local file is still automatic, with no human-verification gate before re-embedding.

### 12.5 State-Specific Law
**DECISION (2026-08-24):** v1's narrow slice is **central law + Karnataka state law**, not central-law-only. `DECISION REQUIRED` (Phase 1): identify which specific Karnataka acts/amendments are relevant to the first-wave domain (criminal law — §12.1) — e.g., Karnataka Police Act, state amendments to criminal procedure, or other state-specific criminal-adjacent legislation — as part of the source-research task below. Karnataka remains the "select state" for the broader multi-domain target in §12.1 unless later revisited.

### 12.6 RAG Pipeline (conceptual, technology-agnostic — no stack chosen yet)
1. **Ingestion:** acquire source documents from decided authoritative source(s).
2. **Validation:** verify document integrity/authenticity against source (e.g., checksum, official publication reference).
3. **Text extraction & cleaning:** structure-aware extraction (preserve section/sub-section numbering — critical for legal citation accuracy, not just prose flow).
4. **Chunking:** must respect legal document structure (section/sub-section boundaries), not naive fixed-length chunking — a chunk split mid-section risks citation and retrieval accuracy failures.
5. **Metadata tagging:** act name, section number, jurisdiction, effective-date range, source provenance, version/checksum, repealed/successor links.
6. **Embedding & indexing:** technology TBD (§19).
7. **Retrieval:** must return chunk + full provenance metadata, never bare text.
8. **Citation binding:** every LLM-generated sentence that references a legal provision must be programmatically bound to a specific retrieved chunk id — not just "the model said it cited this." **IMPLEMENTED for Legal Strategy (2026-08-24) and Document Generation (2026-08-25):** both agents self-report which retrieved `chunk_id`s they actually cited; the orchestrator checks that list against real retrieved provisions and strips/flags anything that doesn't match, rather than trusting it. This verifies the self-reported citation *list*; it does not (yet) verify that a fabricated section number is actually absent from the generated prose itself — that would need sentence-level entailment checking against source text, which §12.7 below still flags as an open mechanism.

### 12.7 Hallucination Prevention for Retrieval
- Retrieval alone is never treated as *proof* a legal conclusion is correct — retrieval finds candidate relevant text; an explicit verification/consistency step (rule-based or a stricter secondary check) should confirm the cited section text actually supports the generated claim before it's shown to the user. `OPEN QUESTION`: exact mechanism (e.g., quote-verification, entailment check) is implementation-phase, but the *requirement* is locked in now.
- If retrieval confidence is low or no good match is found, the system must say so explicitly rather than presenting a weak match as authoritative.

**IMPLEMENTED, partial (2026-08-26):** `tests/test_citation_accuracy.py` adds a real automated self-consistency check — does every ingested chunk's `section_number` metadata label actually match the text at the start of its own chunk — run against the full real corpus (5,136 chunks), not a sample. This is weaker than the OPEN QUESTION's full entailment check (it verifies metadata-to-own-text consistency, not that generated prose accurately represents a cited section's meaning) but it is real and it already paid off: it caught an actual corpus bug on its first run (a stray "Section 122." cross-reference in `CrPC_1973.pdf` absorbing the real §374 header text into a mislabeled chunk, due to the ingestion regex's `\s*` gap spanning newlines) — fixed in `app/ingestion.py`, corpus re-ingested (5,091 → 5,136 chunks). The full entailment-style check this OPEN QUESTION originally asked for remains not built — it would need an LLM-judge pass and a working `ANTHROPIC_API_KEY`, which this dev environment doesn't have. A companion adversarial test (`tests/test_adversarial.py::test_known_gap_fabricated_section_embedded_in_prose_is_not_caught`) proves this remaining gap concretely: a fabricated section number embedded directly in generated prose (outside the separately-checked `citations_used` list) survives completely unverified.

## 13. Case Law & Precedent Reference (Similar Case Finder)

### 13.1 Capabilities Envisioned
Similar factual-pattern search, similar-provision search, precedent retrieval, comparison/explanation of similarities & differences, jurisdiction/court-level relevance, and status tracking (overruled/distinguished/followed) where available.

### 13.2 Known Limitations (must be designed around, not hidden)
- AI-based "similar case" search is prone to superficial pattern-matching (same words, different legal significance) — this must be disclosed to users, not presented as legal-equivalence.
- Overruled/distinguished/followed status tracking requires a maintained citator-like data source, which is a significant undertaking (`OPEN QUESTION`: is such a data source available/licensable, or is this feature descoped for v1?).
- Precedent value varies enormously by court level and jurisdiction — a High Court judgment from a different state may have persuasive-only, not binding, value. The system must always surface court level and binding/persuasive status alongside any case reference.

**DECISION (2026-08-21), briefly reversed 2026-08-24, RE-CONFIRMED DEFERRED 2026-08-24:** Case-law/precedent retrieval is **deferred to a later phase**, not in v1 — including for the criminal-law slice. It was briefly brought into v1 scope as document-hierarchy tier 4 (§12.1), but Phase 1 research confirmed no true citator (a maintained overruled/distinguished/followed tracking source) exists at a cost/access level realistic for a portfolio project: Indian Kanoon's API exposes only raw cite/cited-by counts, not treatment classification, and real citator products (SCC Online, Manupatra) are subscription-tier-only. Rather than ship an unreliable version of §13.2's status-tracking requirement, the user chose to descope Tier 4 entirely and keep the original 2026-08-21 deferral in force. v1 focuses exclusively on statutory retrieval (§12) across all criminal-law tiers.

## 14. Question & Answer Preparation System

- Generates: questions the opposing side might ask, clarification questions, witness questions, complainant/respondent questions, follow-ups, and suggested responses.
- **Hard constraint:** Suggested responses must be strictly grounded in facts already present in the Case Builder. The system must explicitly flag questions it cannot yet help answer due to missing evidence/information, rather than fabricating a plausible-sounding answer.
**DECISION (2026-08-24):** Defaults to plain language for a self-represented user, not legal-professional jargon — the more conservative choice given multi-persona support (§3), since plain language degrades better for a professional reader than jargon does for a lay one. Implemented in `backend/app/agents/question_preparation.py`; the hard-grounding constraint above is enforced by requiring `suggested_response: null` + an explanatory gap note whenever a response can't be built from known facts, verified by a test.

## 15. Case Strength Analysis

### 15.1 Explicit Rejection of Naive Scoring
Per the brief: no simplistic numerical "probability of winning" without a strong, defensible methodology — and no such methodology currently exists for this problem at the fidelity this system could achieve. This is a firm design constraint, not a stylistic preference.

### 15.2 Recommended Alternative Presentation (to be validated with user)
Structured, multi-dimensional, qualitative-first presentation:
- Evidence coverage per claim (supported / partially supported / unsupported).
- Enumerated contradictions and disputed facts.
- Enumerated legal uncertainty points (e.g., "multiple plausible classifications, not yet resolved").
- Enumerated counterarguments and their apparent strength (qualitative: strong/moderate/weak rationale shown).
- Explicit confidence/uncertainty language per section, never a single aggregate score.

**DECISION (2026-08-24):** Yes — one coarse categorical band (`early-stage` / `partially-documented` / `well-documented`), shown alongside the full disaggregated detail, never instead of it. Computed by a pure deterministic function of already-known counts (evidence-supported ratio, unresolved contradictions, classification-hypothesis count) — no LLM involvement in computing the band itself, so it can't be talked into a rosier answer than the underlying data supports. Ships with an explicit disclaimer that it is a documentation-completeness signal, not a probability of success or a legal verdict — the over-interpretation risk this question flagged is mitigated by the disclaimer and by keeping the full detail always visible, not by omitting the band.

## 16. Explainability, Citation, and Hallucination Prevention

- Every legal-provision reference shown to a user must display: act name, section, jurisdiction, effective-date range, and a link/reference to the exact retrieved source chunk.
- Every agent-generated conclusion (classification, strategy option, counterargument) must be traceable to the specific facts/evidence/provisions that produced it — not just presented as an opinion.
- The system must have a well-defined behavior for **uncertainty**: explicit "I don't have enough information" / "sources conflict" / "no directly applicable provision found" states, distinct from confident answers. `DECISION REQUIRED`: exact UX pattern for surfacing uncertainty (inline flags, confidence bands, separate "needs clarification" section) — deferred to UI/UX design phase.
- Retrieval ≠ correctness (see §12.7) — this principle applies system-wide, not just to law retrieval; e.g., a "similar case" retrieval is not proof of precedential relevance either.

## 17. Jurisdiction & Temporal Validity

- Jurisdiction (which law applies) and temporal validity (which *version* of the law applies, based on event date) are treated as **first-class, mandatory fields** on every legal conclusion — not afterthoughts.
- The distinction between "identifying potentially applicable provisions" and "giving definitive legal advice" must be reflected in UI language throughout (e.g., "may be relevant," "a court would need to determine," never "you will win" or "this is illegal").

## 18. Privacy, Security, and Data Handling

**DECISION (2026-08-21):** No authentication required for v1 — session-only, anonymous use. Case data is tied to a browser/session, not a persistent account. Consequences to design for:
- Case data does **not** persist reliably across sessions/devices unless a separate mechanism is added (`OPEN QUESTION`: is any session-persistence — e.g., a local export/save-and-resume — needed, or is a case fully ephemeral within one sitting?).
- Weaker privacy posture than an authenticated system in one sense (no account-level access control) but also a smaller attack surface (no credential storage, no account-takeover risk) — consistent with portfolio-project scope (§3.1) and synthetic/demo data assumption.
- Audit logging (§7) still applies to session data while it exists, even without persistent accounts.
- This decision should be revisited before any production-track evolution of the project (§23) — session-only auth is not adequate if real users' real legal data is ever handled long-term.

**Research findings (2026-08-24, background agent) informing the decisions below** — confidence levels as given by the research, not independently re-verified:
- India's DPDP Act 2023 has **no separate "sensitive personal data" category** (moderate-high confidence) — unlike GDPR or the old SPDI Rules, it applies one uniform standard to all personal data. Legal Lens's criminal/POCSO narrative content gets **no automatic extra statutory protection** beyond ordinary personal data. §18's "assume worst-case sensitivity by default" design principle is therefore *more* protective than what DPDP legally requires, not merely compliant with it.
- Cross-border transfer to a US-based LLM API (Anthropic) is likely permitted under DPDP's blacklist model (no blanket restriction found against the US), but requires informed consent — clear notice of what data goes where (moderate confidence).
- **POCSO §19 mandatory reporting is a genuinely unresolved tension, not a solved problem** (high confidence on the statutory text, moderate on application here): §19 imposes a personal duty on "any person" with knowledge of a child-sexual-offence to report to police — this is a personal obligation on individuals, not organizations, requiring actual knowledge/reasonable belief. No source found addresses whether this creates a duty for people operating an AI tool that merely processes such narratives. **Not resolved by this project — flagged as a live open legal question**, not decided away.
- **Section 228A IPC / BNS §72 (victim-identity disclosure)** criminalizes publishing/disclosing a sexual-offence victim's identity, with narrow exceptions. Any export, sharing, or display of a case file naming a minor or sexual-offence victim is a concrete exposure — independent of DPDP, and relevant to any future export/sharing feature.
- No India-specific AI/legal-tech regulatory guidance exists yet (BCI has issued none for consumer-facing tools as of this research); the Advocates Act's "practice of law" concept centers on court appearance/representation, which Legal Lens's design (no filing, no appearance, no unqualified imperative language, review-required drafts) stays outside of by the traditional definition — but this boundary has not been tested for AI tools specifically by any court or regulator.

**DECISION (2026-08-24):** Given the above, and given §3.1's already-standing decision that this is a portfolio project using synthetic/demo data by default (not real users' real legal matters):
- **Sensitive-domain handling policy**: POCSO stays in the ingested statutory corpus as a retrievable legal reference (already downloaded, part of the criminal-law domain) — the product answers "what does POCSO say," it does not attempt to detect, flag, or act on live child-safety disclosures in a narrative. No keyword-based content-detection or reporting flow is built. The unresolved §19 mandatory-reporting question above means this product **must not be used with real users' real ongoing-abuse narratives** without separate legal review — this is a hard boundary for the portfolio scope, not a future nice-to-have.
- **Data residency**: not resolved — no research done on this specifically; remains open, deferred to any production-track evolution (§23), since portfolio-scope data is synthetic/demo by design.
- **Encryption at rest — DESIGN (2026-08-24); evidence files IMPLEMENTED (2026-08-26), DB encryption explicitly skipped**: original target design — SQLite via `SQLCipher` for case data, evidence files encrypted individually with `Fernet`. **User was asked directly** (real evidence content had been sitting unencrypted on disk since file upload shipped, making this a live gap, not a hypothetical one) which parts to build: full SQLCipher-plus-Fernet, Fernet-evidence-only, or skip. **Decision: Fernet-encrypt evidence files only.** Implemented in `backend/app/encryption.py`: key from `EVIDENCE_ENCRYPTION_KEY` env var if set, else auto-generated and persisted to a gitignored `data/.evidence_encryption_key` for dev-session continuity — key management for a real deployment (secrets manager/KMS) remains explicitly unresolved, per the original note, not silently declared solved by this. `evidence_extraction.py` was refactored to operate on raw bytes rather than a file path, so text extraction happens on plaintext in memory *before* encryption — a plaintext copy of an uploaded evidence file never touches disk, not even transiently. Verified live: `tests/test_encryption.py` uploads a real file through the real endpoint and confirms the on-disk bytes are not the plaintext (file-format magic bytes absent) and round-trip correctly through `decrypt_bytes()`. **The SQLite DB itself (case/claim/statement/evidence metadata) remains unencrypted, and ChromaDB's persistence remains unencrypted** — SQLCipher requires a different Python driver and connection-string changes across `db.py`/`config.py`, judged (and reconfirmed by the user) disproportionate for a local-only dev build with no real user data.
- **Encryption in transit**: not implemented at the application layer — assumes a TLS-terminating reverse proxy in any real deployment, which doesn't exist for this local/dev-only build. `http://localhost` traffic is unencrypted by nature of local dev.
- **Data retention — DESIGN (2026-08-24), explicitly NOT implemented (re-confirmed 2026-08-26)**: target design remains a `DELETE /cases/{id}` endpoint leaning on the existing cascade relationships. **User was asked directly** which of two real options to build — hard-delete everything including the audit log (simple, no schema change) vs. hard-delete case data but decouple and retain audit-log entries for compliance (requires a schema change, and bakes in an answer to the soft-delete-vs-hard-delete/audit-trail-survival tension this section already flags as a genuine, unresolved conflict) — or to skip entirely. **User chose to skip.** This is now a confirmed decision not to build a deletion mechanism at this time, not an unreached one.
- **Audit logging**: **implemented 2026-08-24** — see §7 and the `AuditLogEntry` model (`backend/app/models.py`); every material Case Builder mutation is logged and retrievable via `GET /cases/{id}/audit-log`. Who can see it: currently anyone with the case_id (no auth, matching §18's session-only decision) — not access-controlled beyond that. Retention: same as case data (indefinite, no auto-deletion — see above).
- **Model training/improvement**: confirmed **no** opt-in — this is a documented decision, not independently verified against Anthropic's current API terms; that verification should happen before any real deployment.

## 19. System Architecture & Technology

**DECISION (2026-08-21):** LLM approach is **API-based hosted models** (not local/self-hosted).

**DECISION (2026-08-24):** Full stack locked in to unblock scaffolding — proposed by the assistant given user authorization to "pick a stack unilaterally where reasonable," not independently deliberated line-by-line:

- **LLM provider: fully local (Ollama + Qwen3.5 9B), switched 2026-08-26 for the second time.** History: Anthropic Claude API (original v1 decision) → Google Gemini free tier (after the Anthropic account had no funded credit balance) → **local, no API/network dependency at all** (at the user's explicit request, to remove external dependency entirely). This is a genuine reversal of §19's original "API-based hosted models (not local/self-hosted)" decision, made knowingly, not by drift. **Hardware verified before choosing a model**, not assumed: MacBook Pro, Apple M3 Pro, 18GB unified memory, confirmed via `sysctl`/`system_profiler` — comfortably sufficient for a 9B model at Q4 quantization (~6.6GB weights). The user already had `qwen3.5:9b-q4_K_M` pulled via Ollama; used as-is rather than downloading something else. Model runs via Ollama's **native** `/api/chat` endpoint (`http://localhost:11434`), not its OpenAI-compatibility layer — Qwen3.5 is a reasoning model whose hidden "thinking" otherwise consumes the entire output budget with empty visible content (verified live: a 200-token budget fully consumed by reasoning, zero answer). Neither `reasoning_effort` nor `extra_body={"think": False}` is honored through the OpenAI-compatible endpoint for this model; only the native endpoint's top-level `"think": false` field actually works (verified: a trivial prompt dropped from a botched 200-token truncation to a correct 4-token answer in ~0.5s). `app/agents/base.py` now calls this directly via `httpx`, not the `openai` SDK (removed as a dependency). No API key of any kind is required — `.env` no longer needs any LLM credential. This is the third provider swap and the third time `_call_model()`'s signature/return-type contract held: every agent's `run()` logic, every system prompt, and all 83 tests (which mock `_call_model` itself) needed zero changes. See the matching `COMPLETION.md` entry and the change log below for the full verification trail, including real live output quality checks against `FactExtractionAgent` and `LegalClassificationAgent` (15–18s per call on this hardware, correct structured JSON, no fabrication).
- **Backend: Python + FastAPI.** Best ecosystem fit for the LLM/agent/RAG-heavy work ahead.
- **Frontend: React + Vite + TypeScript.** Standard, fast local dev loop.
- **Primary datastore (Case Builder): SQLite.** Matches the local/dev, portfolio-scale, session-only-auth decisions already made (§3.1, §18) — no server process to run.
- **Vector database: ChromaDB (embedded).** No separate service to stand up; sufficient for one domain's statutory corpus.
- **Knowledge graph layer: not included for v1.** `OPEN QUESTION` resolved as "not worth the complexity" by default given portfolio scope — revisit only if entity/relationship modeling in the Case Builder proves inadequate with the relational model alone.
- **File/evidence storage: local filesystem**, under the repo's data directory. Encryption-at-rest is still an open §18 item — not implemented in the initial scaffold; flagged, not silently assumed solved.
- **Deployment target: local/dev only.** Consistent with §3.1's portfolio-scale framing.
- **API design: REST.** Simplest fit for a hand-coded orchestrator (§8.1) driving synchronous per-turn agent calls.

**UPDATED 2026-08-26 (second reversal):** the API-based-LLM decision above no longer holds — the LLM provider is now fully local (Ollama + Qwen3.5 9B, running on-device). Sensitive user narrative content **no longer leaves the machine at all**, which genuinely resolves the cross-border-transfer and model-training-opt-in concerns flagged below for Anthropic, then re-flagged as unverified for Google — there is no third party to send data to, no cross-border question, no training-opt-in question, because there is no API call. This is a real, structural privacy improvement, not just a cost-driven convenience — worth stating plainly rather than only as a side effect of removing rate limits. The one caveat: this only holds for the LLM calls themselves — ChromaDB and SQLite still store case data locally in plaintext (evidence *files* are Fernet-encrypted, per §18 above; the databases are not), so "no data leaves the machine" is not the same as "the machine's own storage is secure," and that distinction should stay explicit in any privacy-facing text.

*(Historical, superseded 2026-08-26): sensitive user narrative content was sent to Google's Gemini API (switched from Anthropic earlier the same day). §18's cross-border-transfer and model-training-opt-in research below was conducted specifically about Anthropic's terms and was never redone for Google before the local switch made the question moot.)*

## 20. Evaluation & Benchmarking Strategy (to be developed)

Planning-stage placeholders — to be filled in once scope is set:
- Retrieval evaluation: precision/recall of correct provision retrieval against a hand-built gold set of legal-scenario → correct-provision mappings.
- Citation accuracy: does every generated citation actually match retrieved source text (automatable check).
- Hallucination testing: adversarial prompts designed to induce fabricated citations or overconfident claims.
- Agent-level evaluation: does Fact Extraction correctly separate fact/opinion/assumption on labeled test narratives; does Legal Classification surface the correct hypothesis set on known scenarios.
- End-to-end case simulations against realistic (anonymized/synthetic) scenarios, ideally reviewed by a person with legal training.

`OPEN QUESTION`: Is legal-expert review available/planned at any point in this project (even informally)? This materially affects how much we can trust our own eval process.

## 21. Risks, Failure Cases, and Misuse Prevention

- **Hallucinated citations** — mitigated by §12.7's binding/verification requirement.
- **Overconfident case-strength presentation** — mitigated by §15's rejection of naive scoring.
- **Users treating output as definitive legal advice** — mitigated by persistent disclaimers, UI language choices (§17), and explicit human-in-the-loop framing (§5).
- **Jurisdiction/temporal mismatch** (applying wrong-era or wrong-region law) — mitigated by §12.3/§17 requirements.
- **Sensitive-domain harms** (e.g., mishandling domestic violence or minor-safety disclosures) — policy decided §18 (2026-08-24): POCSO stays as retrievable statutory reference only, no detection/reporting flow; product must not be used with real ongoing-abuse narratives given the unresolved §19 mandatory-reporting question.
- **Personal mandatory-reporting exposure for operators** (2026-08-24 research finding, not previously tracked): POCSO §19 imposes reporting duties on "any person" with knowledge of a child-sexual-offence — untested whether this reaches individuals operating a tool that processes such narratives. Mitigated only by scope (synthetic/demo data, §18's hard boundary against real-user use in this domain), not by any technical safeguard.
- **Victim-identity disclosure liability** (2026-08-24 research finding): Section 228A IPC / BNS §72 criminalizes disclosing a sexual-offence victim's identity. Any future export/sharing/logging-display feature touching case files with a named minor or sexual-offence victim needs this considered explicitly — not yet a concern since no export feature exists, but flagged before one is built.
- **Misuse for generating bad-faith legal threats/harassment documents** — `OPEN QUESTION`: what safeguards (rate limits, review gates, misuse detection) are appropriate for the Document Generation agent? Still open — Document Generation is implemented (§8.7) but no misuse safeguard was added; watermarking (the one safeguard that existed) was removed 2026-08-24 at the user's explicit instruction, which widens this risk rather than narrows it.
- **Over-collection of sensitive personal/legal data** without adequate security — see §18. Encryption at rest/in transit and data retention are confirmed **not implemented** (§18, 2026-08-24), not just unresolved in the abstract.

## 22. Human-in-the-Loop Points (candidates, to be refined)

- Before any generated document (complaint/notice/etc.) is treated as "final" — always requires explicit user acknowledgment that it's an AI draft. **Status (2026-08-24): not currently enforced.** The watermarking/labeling mechanism that would have carried this acknowledgment was removed from Document Generation at the user's explicit instruction (§8.7's DECISION note) — this human-in-the-loop point is now aspirational text, not an implemented behavior.
- `OPEN QUESTION`: Is there ever a point where a *licensed professional* review is built into the product flow (e.g., an optional lawyer-review marketplace/handoff), or is that entirely out of scope? This is a product-strategy question, not just a technical one.

## 23. Future Expansion (explicitly NOT v1 — parking lot)

- Broader statutory coverage (state laws, additional central acts).
- Case-law/precedent search (see §13, likely deferred).
- Multi-jurisdiction support beyond India.
- Multi-language support — **DECIDED (2026-08-21): English only for v1**; Hindi/regional-language support explicitly deferred to future expansion.
- Lawyer-review marketplace / professional handoff integration.
- Deeper evidentiary content analysis (e.g., automated contract clause analysis) beyond evidence logging.
- Knowledge graph-based cross-referencing across statutes and case law.

---

## Change Log

- **2026-08-21** — Initial draft created. No user-confirmed decisions yet; all `DECISION REQUIRED` / `OPEN QUESTION` / `ASSUMPTION TO VALIDATE` items are pending the discovery interview.
- **2026-08-24** — Phase 0 closed. §2 problem statement and §5 product boundaries confirmed as drafted, no changes. §12.1 sequencing recommendation upgraded from proposal to `DECISION`: narrow-first ingestion confirmed, broad multi-domain remains the target architecture. Per-domain legal/ethical risk enumeration deferred to Phase 1 (needs first-wave domain selection first). Phase 1 begun.
- **2026-08-24** — Phase 1 discovery round 1: first-wave domain decided as **criminal law** (BNS/BNSS/BSA + historical IPC/CrPC/Evidence Act) (§12.1) — raises §12.3's dual-code problem and §18's sensitive-domain policy gap earlier than originally recommended, flagged for near-term research. State-law scope decided as **central + Karnataka** (§12.5), not central-only as previously recommended; specific Karnataka criminal-adjacent acts still to be identified during source research.
- **2026-08-24** — Phase 1 research round 1 (background agent): identified 4 Karnataka criminal-law-adjacent acts (Police Act 1963, KCOCA 2000, Prevention of Dangerous Activities/"Goonda" Act 1985, Police Complaints Authority rules); evaluated candidate sources and proposed a hierarchy. User then expanded criminal-law v1 scope to a 5-tier document hierarchy (core legislation, central special laws, state laws, judicial precedents, execution/police manuals) and **reversed §13.2's precedent-retrieval deferral for the criminal-law domain specifically** — see §12.1 and §13. Source-authority hierarchy locked in as e-Gazette > India Code > Karnataka DPAL > Indian Kanoon (paid API) (§12.2); acquisition method decided as manual human download rather than scraping, sidestepping the unverified ToS gap. Tiers 2 and 5 (special laws, police manuals) still need their own source research pass before Phase 3.
- **2026-08-24** — Phase 1 research round 2 (background agent): investigated Tier 4 (citator availability) and Tier 5 (police manuals sourcing). Found no true citator exists at portfolio-project cost, and no public source for a Karnataka Police Manual/Standing Orders. User then **descoped both Tier 4 and Tier 5**, re-confirming §13.2's original precedent-retrieval deferral rather than shipping an unreliable version of it. Criminal-law v1 document scope is now a **3-tier** hierarchy: core legislation, central special laws, Karnataka state laws (§12.1).
- **2026-08-24** — Phase 1 discovery round 2: §12.3's effective-date/successor-pointer data model confirmed as a firm requirement (was an assumption). §12.4's amendment-tracking strategy decided as **automated periodic re-check**, overriding the doc's own manual-review-gated recommendation — risk flagged (silent automated ingestion of a misread amendment conflicts with §7/§16), with a Phase 2 follow-up to define whether "automatic" means full silent ingestion or automated detection + human-gated application.
- **2026-08-24** — User directed a jump straight to code scaffolding, explicitly deferring (not dropping) Phase 1's remaining practice-of-law and privacy research, and explicitly skipping domain risk enumeration. Phase 2 stack and architecture decisions made to unblock this (§8.1, §8.8, §19): hand-coded orchestrator state machine, shared Case Builder state for agent communication, synchronous per-turn invocation; Python/FastAPI backend, React/Vite/TypeScript frontend, Anthropic Claude API, SQLite, embedded ChromaDB, local filesystem evidence storage, local/dev deployment, REST API. Encryption-at-rest for evidence storage explicitly NOT implemented yet — flagged as a real gap, not silently assumed solved. Repository scaffolding follows this entry.
- **2026-08-24** — Acquisition method reversed: user asked the assistant to download the criminal-law corpus rather than doing it manually. Robots.txt was checked (not assumed) before proceeding — Karnataka DPAL permits crawling, India Code was inconclusive but only single direct document URLs were fetched, not bulk crawling. All 13 available documents downloaded, each verified to have real extractable text (one scanned-image IPC download caught and redone). Ingested: 5,091 chunks. Law Retrieval verified live returning real hits (BNS §103 for "punishment for murder" query) instead of `insufficient_data`. Full per-document provenance, including sources that diverged from the original plan and two secondary-sourced Karnataka acts flagged for re-verification, recorded in `MANIFEST.md`. See §12.2's updated decision.
- **2026-08-24** — Legal Strategy and Complaint/Document Generation agents implemented, completing the full §8 agent roster. Legal Strategy resolves §8.5's previously-open gating question: ranked recommendations now require explicit `acknowledged: true` before being returned. Document Generation handles all 7 draft types via one parameterized agent. **§8.7's watermarking/AI-draft-labeling requirement then removed at the user's explicit instruction** — see the new `DECISION` note under §8.7. This is a real narrowing of the product's legal-risk posture (§5, §22), documented rather than left implicit.
- **2026-08-24** — Dynamic Interview system implemented (§9): question generation (incorporating live Legal Classification hypotheses), a hybrid stopping criterion resolving §9.2's open question (fixed budget + agent soft-signal + user can end early), and contradiction flagging against prior statements/answers (§9.3). New `InterviewTurn` entity added to the Case Builder schema, outside §10.2's original sketch — flagged there, not silently added. A real bug was caught by the test suite and fixed: the orchestrator was calling the Legal Classification agent unconditionally, even before checking whether the case had any facts yet — now short-circuits on an empty case, matching the pattern already used elsewhere (Devil's Advocate, Document Generation). §9.4 (jurisdiction detection) remains genuinely open, not addressed by this work.
- **2026-08-24** — Case Strength analysis implemented (§15), closing out Phase 4. Not a new agent — a synthesis layer over what other agents already produce (Evidence Gap Analysis's deterministic status, persisted contradiction flags, Legal Classification hypotheses, Devil's Advocate weaknesses now extended with a `severity` field). §15.2's open question resolved: yes to one coarse categorical band, computed deterministically (no LLM), always shown alongside full disaggregated detail with an explicit non-verdict disclaimer — §15.1's rejection of naive numeric scoring stays intact, verified by a test that walks the entire response tree for forbidden score-shaped keys. Another instance of the same bug class caught in Dynamic Interview was found and fixed at its shared source (`classify_and_retrieve()` calling Legal Classification unconditionally on empty cases), benefiting three callers at once rather than three separate patches.
- **2026-08-24** — Frontend caught up to the backend: a typed API client and one panel per agent for all 13 endpoints, including a real UI for Legal Strategy's acknowledgment gate (§8.5) — not just a backend flag. **Verified live in a browser**, not just type-checked: both dev servers started, driven with headless Chrome via Playwright (the `run` skill's preferred `chromium-cli` wasn't available in this environment; used the documented fallback), all panels screenshotted rendering with zero console errors, a claim added end-to-end against the real backend, and the narrative-submission failure path (no API key configured here) confirmed to fail gracefully rather than crash. **A real bug was caught by this live check, not by inspection**: the Legal Strategy disclaimer shown to end users literally read "Re-request with acknowledged=true" — raw API parameter syntax in user-facing copy — found only by actually looking at the rendered screenshot; fixed in `orchestrator.py`.
- **2026-08-24** — User asked to work through the entire remaining backlog. Large batch of real progress across every phase, not just documentation cleanup:
  - **Versioning + audit logging (§7, §10.2)**: new `AuditLogEntry` model, one append-only log doing double duty for both requirements rather than building two mechanisms. Wired into every material mutation (narrative, claims, evidence, documents, interview answers). New endpoint `GET /cases/{id}/audit-log` + `AuditLogPanel`, verified live to show a real logged event after adding a claim through the UI.
  - **Citation binding (§12.6)**: `Orchestrator._bind_citations()` now programmatically checks Legal Strategy's self-reported citations against actual retrieved `chunk_id`s from ChromaDB (added to the retrieval pipeline for this purpose) and strips anything fabricated — verified by a test with one real and one deliberately-fabricated citation, confirming only the real one survives. Not yet extended to Document Generation — flagged as a remaining gap, not silently left uncovered.
  - **Timeline (§10.2)**: `Orchestrator.timeline()` sorts events with parseable dates, separates unparseable/approximate ones rather than guessing. New endpoint + `TimelinePanel`, verified live.
  - **Question & Answer Preparation (§14)**: new agent resolving the section's open audience question (plain-language default) and enforcing its hard grounding constraint (`suggested_response: null` + gap note when a response can't be built from known facts). New endpoint + `QuestionPreparationPanel`, verified live in a browser.
  - **Retrieval evaluation, first pass (§20)**: `tests/test_retrieval_accuracy.py` runs 5 hand-verified queries against the REAL ingested corpus (not mocked) — every expected answer was confirmed by directly inspecting live query output before writing the assertion. Also **discovered and documented a real weakness** rather than hiding it: POCSO/NDPS queries return amendment-notice chunks instead of substantive offence text, a naive-chunker limitation.
  - **Real bug found and fixed via live testing, not code review**: an unhandled backend exception (e.g. missing API key) propagated past `CORSMiddleware`, so the browser reported a misleading "blocked by CORS policy" error instead of the actual failure. Added a global exception handler with proper CORS headers + server-side logging; verified both by a unit test and a live browser re-run showing the confusing CORS message is gone.
  - **§18 security/privacy policy resolved**, informed by dedicated research into India's DPDP Act, the Advocates Act's practice-of-law boundary, and — the most consequential finding — a genuinely unresolved POCSO §19 personal mandatory-reporting question and a Section 228A IPC/BNS §72 victim-identity-disclosure risk, neither previously tracked. Resulting policy: POCSO stays as a retrievable statutory reference only, no detection/reporting flow, and the product **must not be used with real users' real ongoing-abuse narratives** without separate legal review — a hard portfolio-scope boundary. Encryption at rest/in transit and data retention are confirmed **not implemented** (real gaps, not silently assumed solved); authorization model decided (case_id possession = access, matching the no-auth design).
  - **Phase 6/9 handled honestly, not pretended-complete**: legal-expert review stays genuinely blocked (no such resource available); final demo/report/benchmarking are correctly treated as end-of-project deliverables, not mid-backlog items — documented as such rather than silently skipped or falsely checked off.
- **2026-08-24** — User asked to complete everything remaining through Phase 4. Real progress across every one of those phases, closing all but one deliberate exclusion (similar-case retrieval, correctly still deferred per prior research):
  - **Security incident caught and fixed mid-session**: the user pasted their real Anthropic API key into `backend/.env.example` (tracked, committable) instead of `backend/.env` (gitignored). Confirmed the key had NOT yet been committed (only a local uncommitted diff), moved it to `.env`, restored `.env.example` to its clean placeholder, and verified `.env` is properly gitignored. A live end-to-end test then confirmed the key itself works — request reached Anthropic successfully — the only blocker is the account's own credit balance, not a setup issue.
  - **Jurisdiction detection (§9.4)**: resolved as both explicit input (`POST /cases` accepts a `jurisdiction` override) and a non-binding keyword-heuristic mismatch warning on narrative submission — never auto-changes the case's jurisdiction.
  - **Inter-agent conflict resolution (§8.8)**: resolved as "surface, don't silently resolve" — `_flag_agent_conflicts()` juxtaposes Legal Classification hypotheses against "strong" Devil's Advocate weaknesses rather than picking a winner, directly implementing §8.1's own worked example.
  - **Real evidence file upload + OCR built** (§11.3), not just designed: `pytesseract` for images, `pypdf` for PDF text layers, an honest `high/medium/low/none` confidence indicator that propagates into Evidence Gap Analysis. Verified with real tesseract OCR against a real generated image (not mocked) — a genuinely blank test image correctly returns `"none"`.
  - **Encryption and data-retention DESIGN specs written** (§18) — SQLCipher/Fernet plan, a `DELETE /cases/{id}` endpoint design leaning on existing cascade relationships — explicitly not implemented, since Phase 2 asks for design and Phase 7 (implementation) remains correctly untouched, not conflated with "done."
  - **Document checksums + change-detection re-ingestion implemented and verified live** (§12.4): SHA-256 per source file; a real run against the full 13-document corpus processed all 13 on first pass, then correctly skipped all 13 as unchanged on a second run. The scheduling half of "automated periodic re-check" is still not built — only the detection half.
  - **Document cleaning** (§12.6): strips page-number lines and excess whitespace from PDF extraction artifacts.
  - **Gold-standard retrieval eval set nearly tripled**: 5 → 14 hand-verified queries, now spanning BNS, IPC, UAPA, KCOCA, and BSA/Evidence Act, not just BNS/IPC.
  - **Phase 1's last three items closed**: India Code's robots.txt confirmed broken (502) on a second check on a different day — treated as a real external blocker, not something more retrying would resolve; Wave 2 domain-expansion candidates decided (Consumer Protection Act, PWDVA, Contract Act, in that priority order).
  - Test suite grew from 36 to 58 tests, all passing throughout.
- **2026-08-25** — User asked to close out everything genuinely still open through Phase 4. Four concrete, previously-flagged gaps closed rather than re-litigating anything already decided: (1) §12.3's effective-date/successor-pointer data model implemented in ingestion and verified live (BNS §103 correctly tagged, IPC §503/506/507 correctly tagged as repealed-by-BNS); (2) a shared JSON retry/repair helper added and applied to Fact Extraction, the one agent explicitly flagged as missing it; (3) §11.3's `partially_supported`/`disputed` claim statuses implemented via a new `Evidence.disputed` flag, closing a gap that had been left open pending "modeling this schema doesn't have yet"; (4) citation binding (§12.6) extended from Legal Strategy to Document Generation, with the same "surface, don't silently trust" pattern. Also corrected one stale tracker note (Dynamic Interview's line still claimed jurisdiction detection was unaddressed, though it had been implemented later the same day it was written). Left deliberately untouched: re-ingestion scheduling infrastructure (no local/dev equivalent worth building), live-source document validation (would require automating the fetch §12.2 deliberately keeps manual), and Phase 6 evaluation work (blocked on either a working API key or the still-unavailable legal-expert review resource). Test suite: 58 → 67, all passing.
- **2026-08-25 (same day, follow-up)** — User asked to explicitly finish closing out Phase 4. The JSON retry/repair helper (added for Fact Extraction above) was extended to Legal Classification, the only other agent still using a bare unrecovered `json.loads`. The two remaining `COMPLETION.md` lines still marked in-progress were held back only by an "unevaluated against labeled scenarios" caveat — correctly Phase 6's own checklist item, not a Phase 4 implementation gap — so that bookkeeping was corrected rather than the evaluation being faked. **Phase 4 (`COMPLETION.md`) is now closed**, with the sole exception of similar-case/precedent retrieval, which remains excluded by the standing 2026-08-21/08-24 scope decision (§13.2), not left incomplete. Test suite: 67 → 69, all passing.
- **2026-08-26** — User asked to close out Phase 5. Two real gaps closed: (1) Evidence organization UI/logic (§10.3/§11) — a new `GET /cases/{id}/evidence` inventory endpoint and a working "Organize evidence" grouped view + dispute toggle in `ClaimsEvidencePanel`, verified live in a real (Playwright-driven Chromium) browser against the real backend — zero console errors. (2) Document Generation's citation-binding line was stale (the binding itself shipped in the prior session but the tracker/frontend hadn't caught up) — `DocumentsPanel` now surfaces the citation-verification fields the backend already returns. The one item left open — AI-draft watermarking, removed at the user's explicit instruction in a prior session — was **not** unilaterally restored to make the checklist look complete; asked directly, and the user confirmed leaving it removed. Phase 5 is closed with that one item standing as a deliberate, now twice-confirmed exclusion. Test suite: 69 → 71, all passing.
- **2026-08-26 (same day, follow-up)** — User asked to verify Phases 0-5 were genuinely complete before starting Phase 6. Re-audited every remaining non-`[x]` line against the actual code rather than trusting prior notes — confirmed all of them are decided exclusions or honestly-flagged, correctly-deferred limitations, nothing silently incomplete. Phase 6 work: built a real citation-accuracy self-consistency check (`tests/test_citation_accuracy.py`) across all 5,136 real ingested chunks, which **caught a genuine chunking bug on its first run** — a stray "Section 122." cross-reference in `CrPC_1973.pdf` was absorbing the real "374. Appeals from convictions" section header into a mislabeled chunk, because the header regex's `\s*` gap spanned newlines. Fixed (`_SECTION_HEADER_RE`'s `\s*` → `[ \t]*`), added a regression test, re-ingested the real corpus (5,091 → 5,136 chunks, confirming the fix helped beyond the one case found first). Also added two adversarial tests (`tests/test_adversarial.py`): one proving citation binding does NOT catch a fabricated section number embedded directly in generated prose (a documented gap, not a fix — binding only checks the separate citations list), and one proving Case Strength's aggregate band can't be pushed to "well-documented" by an unresolved contradiction alone. Agent-level LLM-quality evaluation, end-to-end simulations, and legal-expert review remain correctly blocked (no working API key, no available legal reviewer) — left open, not faked. Test suite: 71 → 76, all passing.
- **2026-08-26 (same day, follow-up)** — User asked to close out Phase 7. Two items carried real security/architecture tradeoffs §18 had already flagged as unresolved — asked the user directly instead of guessing. **Encryption**: offered Fernet-evidence-files-only (matches the original design, scoped) vs. full SQLCipher-plus-Fernet vs. skip; user chose evidence-files-only. Implemented in new `backend/app/encryption.py` (the `cryptography` package, newly added as a dependency) — a key is loaded from `EVIDENCE_ENCRYPTION_KEY` if set, else auto-generated and persisted to a gitignored file for dev-session continuity, with real key management explicitly still unresolved. `evidence_extraction.py` refactored to work on raw bytes rather than a file path so extraction happens on plaintext in memory before encryption — no plaintext copy touches disk. Verified live with a real file upload through the real endpoint, confirming the on-disk bytes aren't the plaintext and round-trip correctly through decryption. This also closed out "secure evidence storage," whose prior "moot, no upload exists yet" note had gone stale since file upload shipped in an earlier session — real unencrypted evidence content had been sitting on disk with nothing flagging it as a live gap. **Data retention**: offered hard-delete-everything vs. hard-delete-but-preserve-audit-log (a schema change baking in an answer to a genuinely unresolved compliance tension) vs. skip; user chose skip — now a confirmed decision, not an open question. **Authorization** was resolved without needing to ask: "case_id possession = access" already *is* the complete decided model for the no-auth architecture, not a partial implementation of something more — closed to match authentication. Test suite: 76 → 79, all passing.
- **2026-08-26 (same day, follow-up)** — User asked to close out Phase 8. One real gap: no designed user flow existed — `App.tsx` was one flat vertical stack of all 9 panels regardless of case stage. Replaced it with a real 3-stage tab flow (Interview / Case Dashboard / Document Review — the exact three flows the tracker itself had named as missing), gated so the Dashboard/Review tabs are disabled until a narrative is submitted, with auto-advance to Dashboard on submission. New tab-bar styles added to `shared.ts`. **Verified live in a browser** (Playwright/Chromium, narrative call mocked since no working API key exists in this dev environment — everything else real): confirmed the gating, confirmed each stage renders only its own panels, zero console errors. The rest of Phase 8's items had simply gone stale across earlier scaffolding-era sessions (panel/endpoint counts, "only fact-extraction wired," "evidence upload is metadata-only" — all long since superseded) — corrected rather than left misleading. Deployment/monitoring setup were reclassified from `[ ]` to `[x]` to match how Authentication is already treated: §19 already decided the deployment target as local/dev-only, so there's no separate step to build for a target that was never chosen — a decision, not a gap. Test suite unchanged at 79/79 (frontend-only work); frontend type-checks and builds cleanly.
- **2026-08-26 (same day, follow-up)** — User asked to close out Phase 9, the final phase. Re-tested the `ANTHROPIC_API_KEY` live rather than trusting old notes — confirmed a genuine `401 authentication_error: API key is invalid`, so full system testing, benchmarking, and legal-expert review remain correctly, permanently blocked (not scoping choices — resources this project was never going to have). Closed everything else: `PROJECT_REPORT.md` (a retrospective — objectives, architecture, phase-by-phase timeline, and a "key technical challenges" section naming the real bugs found across the project's life), `DEMO.md` (a live-presentation script, explicit about which steps need a working API key vs. which don't — including a verified-live no-key retrieval demo against the real corpus), and a documentation-consistency pass across `CLAUDE.md`/`COMPLETION.md`/`explanation.md` (test/chunk/endpoint/document counts re-verified live, `explanation.md`'s frontend section corrected for the tab-flow work from the previous session). **This closes the project's tracked backlog**: every phase from 0 through 9 is now either fully closed, closed except deliberate/confirmed exclusions, or honestly blocked by an external dependency this project never had access to.
- **2026-08-26 (same day, follow-up)** — With the funded-API-key blocker confirmed genuinely unresolvable on the Anthropic side, user asked for free/local alternatives. Offered Gemini's free tier (via its OpenAI-compatible endpoint, minimizing code churn since `_call_model()`'s signature could stay identical) and local options (Ollama + Qwen2.5/Llama 3.x, flagged as a reversal of the standing "API-based, not local" decision if chosen); user chose Gemini. **§19's LLM-provider decision reversed**: Anthropic Claude API → Google Gemini API (`gemini-2.5-flash`), implemented as a real, tracked swap — `app/config.py`, `app/agents/base.py` (new `openai.OpenAI` client pointed at Gemini's compatibility endpoint), `pyproject.toml`, `.env`/`.env.example` all updated; every agent's `run()` logic and every one of the 79 tests needed zero changes, since only `_call_model`'s internals changed, not its contract. **Bug caught by immediately re-running the test suite**: `openai.OpenAI()` validates a non-empty `api_key` at construction time (the old Anthropic SDK didn't), so every agent's `__init__` crashed with the key still blank — fixed with a placeholder fallback preserving the old fail-at-call-time-not-construction-time behavior. 79/79 passing again. Flagged, not silently assumed: §18's data-transfer/training-opt-in research was Anthropic-specific and has not been re-verified for Google.
- **2026-08-26 (same day, follow-up)** — User supplied a real `GEMINI_API_KEY` and correctly caught that the model name should be `gemini-3.6-flash`, not `gemini-2.5-flash`. Verified rather than trusted blindly: called the live API with the old name first and got a genuine `404` from Google confirming it was retired for new users and naming `gemini-3.6-flash` as the replacement — updated `config.py`'s default accordingly. Live-testing the real model then surfaced two genuine integration issues neither Anthropic's SDK nor documentation-reading would have caught: (1) `gemini-3.6-flash` is a reasoning model whose hidden "thinking" tokens count against `max_tokens`, observed consuming ~70% of a 2048-token budget on one realistic prompt — fixed by passing `reasoning_effort="low"` on every call (verified: ~4x fewer total tokens, no quality loss on this project's short JSON outputs, confirmed against real `FactExtractionAgent`/`LegalClassificationAgent` calls). (2) Gemini sometimes wraps JSON in a markdown code fence despite every system prompt forbidding it — fixed once at the shared source (`Agent._call_model()`'s new `_strip_code_fence()`), covering all 9 agents regardless of whether they use the newer retry-repair helper or a bare `json.loads`. New `tests/test_code_fence_stripping.py` (4 tests). Test suite: 79 → 83, all passing. **The pipeline is now confirmed working end-to-end against the real Gemini API** — this unblocks Phase 6/9's live-LLM work in principle; a full evaluation pass hasn't been run yet, but the plumbing is verified real, not assumed.
- **2026-08-26 (same day, follow-up)** — User asked for a fuller end-to-end pass to properly close Phase 6/9's evaluation items. New `backend/e2e_smoke.py` drove a realistic criminal-intimidation narrative (with a deliberately planted contradiction) through the real FastAPI app, real dev DB, real ChromaDB corpus, and real Gemini API — no mocking anywhere. **10 of 13 stages completed with genuinely strong, legally coherent output**, verified live for the first time rather than by mocked assertion: the dual-code retrieval requirement (§12.3) returned both BNS §351 and historical IPC §503 for the same live query; Dynamic Interview correctly caught the planted contradiction; Devil's Advocate correctly flagged it as "strong" severity; Legal Strategy's citations were genuinely bound to real retrieved chunks with zero fabrication; Question Preparation correctly returned `null` + a real gap note for the two questions that weren't actually grounded, rather than fabricating answers. The run stopped short of Document Generation/Case Strength/Audit Log — not a bug, but a real external constraint newly discovered: Gemini's free tier enforces a **daily** quota of 20 requests/day per model, confirmed from the API's own `429 RESOURCE_EXHAUSTED` response. Documented in the script's own docstring so a re-run isn't surprised by it. Phase 6/9 tracker lines updated to reflect substantial, real completion rather than the earlier "blocked" status.
- **2026-08-26 (same day, follow-up)** — User asked to go fully local, no API at all, and to pick a model that actually fits their laptop. Checked real hardware before recommending anything (`sysctl`/`system_profiler`: MacBook Pro, Apple M3 Pro, 18GB unified memory) and checked what was already installed rather than assuming — found Ollama already running and `qwen3.5:9b-q4_K_M` (6.6GB) already pulled, confirming the user's own recollection of "qwen 3.5 9b" was correct. Used it as-is. **§19's LLM-provider decision reversed a second time**: Gemini free tier → fully local, no network dependency. Verified the naive approach first, found it insufficient, then fixed it: Qwen3.5 is a reasoning model whose hidden "thinking" fully consumed a 200-token test budget with zero visible output — neither `reasoning_effort` nor `extra_body={"think": False}` worked through Ollama's OpenAI-compatible endpoint for this model; only Ollama's **native** `/api/chat` endpoint's top-level `"think": false` field actually disabled it (verified: a trivial prompt dropped from a botched truncation to a correct 4-token answer in ~0.5s). Rewrote `app/agents/base.py` to call Ollama's native endpoint directly via `httpx` (removed the `openai` SDK dependency entirely — no longer needed by any provider), updated `config.py` (`ollama_base_url`/`ollama_model`, no API key setting at all), `pyproject.toml`, `.env`/`.env.example`. Updated the one test that inspected the old client's internal structure (`test_code_fence_stripping.py`) to match the new `httpx`-based shape. **Verified real, live, end-to-end**: `FactExtractionAgent` and `LegalClassificationAgent` both called for real against the actual local model, both returning correct, well-structured, legally coherent JSON (15–18 seconds per call on this hardware) — not assumed from the isolated smoke tests alone. §18 updated: this also structurally resolves the cross-border-transfer/training-opt-in privacy questions that had been flagged unresolved for both prior providers, since no data now leaves the machine for LLM calls at all (though local SQLite/ChromaDB storage itself remains unencrypted, a distinct and still-open point). 83/83 tests passing throughout.
- **2026-08-26 (same day, follow-up)** — User asked what was actually wrong with Document Generation, rather than letting the prior "model capability limit" explanation stand unexamined. It turned out to be wrong, and investigating it properly found a real, fixable bug: calling `DocumentGenerationAgent._call_model()` directly with the exact failing scenario's payload showed the model losing track of the drafting task entirely and echoing back a fragment of one of its own *input* provisions as output — triggered specifically by **15 retrieved provisions** (5 hits × 3 hypotheses, heavily overlapping and mostly irrelevant) overwhelming the prompt with noise, not by any hard limit of the model itself. Fixed in `Orchestrator._flatten_retrieved_provisions()`: deduplicate by `chunk_id` and cap both the provision count (8) and each provision's text length (800 chars) before any agent sees them — this also benefits Legal Strategy, which consumes the same flattened list. Re-verified against the exact real failing scenario: Document Generation now produces a correct, well-structured, correctly-cited complaint draft. A third full `e2e_smoke.py` run then completed cleanly end-to-end with **all 9 of 9 agents** producing genuine, good output — not 8 of 9 as previously reported. Corrected the "capability limit, not a bug" framing across every tracker document rather than leaving an incorrect explanation on record — a reminder that the honest move when a plausible-sounding explanation hasn't actually been verified is to go verify it, not to write it down as settled. New `tests/test_retrieved_provisions_capping.py`. Test suite: 84 → 87, all passing.
- **2026-08-26 (same day, follow-up)** — User asked for a full project audit (every file checked, unwanted things deleted, everything verified working) and a `run.py` one-command launcher. Real cleanup, not cosmetic: removed 45 leftover test-artifact evidence files, 4 `.DS_Store` files, `frontend/src/App.css` (184 lines of dead default-Vite-template CSS matching nothing in the real app) and its import, 4 unused image assets, a dead CSS selector, and the `anthropic`/`openai` Python packages still installed from the two earlier provider switches despite no longer being dependencies — uninstalled and the full test suite re-run (87/87 still passed) to confirm they were genuinely unused, not silently load-bearing. Verified live afterward: tests pass, frontend builds (CSS bundle shrank 4.10kB → 1.77kB), ChromaDB intact (5,136 chunks), Ollama reachable, and a real case created through the full live stack end-to-end. New `run.py` at the project root: an idempotent one-command launcher (venv/deps/`.env`/ingestion/`npm install` each checked and skipped if already done, then both servers started together). Found and fixed two real bugs by testing it rather than just writing it — non-tty stdout buffering hid the script's own status output when redirected to a file (fixed with `sys.stdout.reconfigure(line_buffering=True)`), and `SIGTERM` (what `kill` sends by default) wasn't handled, which would have orphaned both subprocesses on anything but a live-terminal Ctrl+C (fixed, verified live with a real `SIGTERM` producing a clean dual shutdown, no leftover processes).
