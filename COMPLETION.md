# COMPLETION.md — Legal Lens Project Tracker

> **Single source of truth for project progress.** This file is maintained continuously as decisions are made and work is completed.
>
> **Maintenance rules (do not violate):**
> 1. Never mark `[x]` unless the task is actually, verifiably complete.
> 2. When a task completes, flip `[ ]` → `[x]` — do not delete or rewrite history.
> 3. Add new subtasks whenever additional work is discovered; do not silently absorb scope into an existing checked item.
> 4. Do not remove incomplete tasks to "clean up" the tracker.
> 5. If a task is blocked, mark it `[ ] (BLOCKED: reason)` and state the dependency explicitly.
> 6. Keep completed tasks visible for historical tracking — this file is a log, not just a live status board.
> 7. Keep this file synchronized with `CLAUDE.md` — every `DECISION REQUIRED` resolved there should unblock/update corresponding tasks here.

**Legend:** `[ ]` not started · `[x]` complete · `[~]` in progress · `[ ] (BLOCKED: ...)` blocked

---

## Phase 0 — Project Definition

- [x] Capture initial project idea and full feature brief from user (2026-08-21).
- [x] Draft initial `CLAUDE.md` master definition document.
- [x] Draft initial `COMPLETION.md` tracker (this file).
- [x] Define problem statement — **CONFIRMED 2026-08-24: draft in `CLAUDE.md` §2 accepted as final** (pre-lawyer orientation and case preparation, not advocacy/adjudication replacement).
- [x] Define target users for v1 — **DECIDED 2026-08-21: multiple personas from day one** (citizen, law student, paralegal, lawyer power-user) (`CLAUDE.md` §3). Follow-up: persona-specific presentation layer still `DECISION REQUIRED`.
- [x] Define jurisdiction scope for v1 — **DECIDED 2026-08-21: India, including select state laws** (`CLAUDE.md` §12.1). Follow-up: which states, on what basis, still open.
- [x] Define legal domain scope for v1 — **DECIDED 2026-08-21: broad multi-domain from the start** (`CLAUDE.md` §12.1). Follow-up: which domains ship in the first working slice vs. later waves still open; now the lead item for Phase 1.
- [x] Define product boundaries / what Legal Lens explicitly will not do — **CONFIRMED 2026-08-24: draft in `CLAUDE.md` §5 accepted as final** (not a lawyer substitute, no outcome guarantees, not an autonomous filer, not a source of law itself).
- [x] Identify legal and ethical risks specific to chosen domain(s) — **DONE**, completed as the Phase 1 item below once criminal law was selected as the first-wave domain (see `CLAUDE.md` §21). This line stays checked here for history; the actual content lives in Phase 1.
- [x] Determine project context — **DECIDED 2026-08-21: portfolio project** (`CLAUDE.md` §3.1). Implication: synthetic/demo case data by default, modest scale targets, compliance work demonstrates awareness rather than production certification.
- [x] Determine expected scale — **DECIDED 2026-08-21: not built for real concurrent user traffic** (portfolio project, `CLAUDE.md` §3.1).
- [x] **RISK FLAGGED — RESOLVED 2026-08-24:** chosen scope (multi-persona + broad multi-domain + India+state law) is large for a portfolio-scale project. **User signed off on narrow-first sequencing**: architecture supports multi-domain from day one, but Phase 3/4 build out one domain end-to-end before expanding to the next (`CLAUDE.md` §12.1).

## Phase 1 — Requirements and Research

- [x] Select first-wave domain for the narrow slice — **DECIDED 2026-08-24: criminal law** (BNS/BNSS/BSA + historical IPC/CrPC/Evidence Act) (`CLAUDE.md` §12.1). Chosen deliberately over the lower-risk consumer-protection recommendation; raises §12.3 (dual-code) and §18 (sensitive-domain policy) sooner.
- [x] Decide state-specific law inclusion — **DECIDED 2026-08-24: central + Karnataka** (`CLAUDE.md` §12.5), not central-only as previously recommended.
- [x] Identify specific Karnataka acts relevant to criminal law — **DONE 2026-08-24** (background research agent): Karnataka Police Act 1963, Karnataka Control of Organised Crimes Act 2000 (KCOCA), Karnataka Prevention of Dangerous Activities Act 1985 ("Goonda Act"), Karnataka Police Complaints Authority rules (`CLAUDE.md` §12.1 tier 3). No Karnataka-specific textual amendments to CrPC/BNSS itself were found — flagged, not assumed absent.
- [x] Research candidate legal data sources (India Code, e-Gazette, Indian Kanoon, licensed databases, etc.) — **DONE 2026-08-24**: compared India Code, e-Gazette, Karnataka DPAL, Indian Kanoon, SCC/Manupatra on authority, licensing, format, and coverage (`CLAUDE.md` §12.2).
- [x] Decide final source hierarchy (which source wins on conflict) — **DECIDED 2026-08-24**: e-Gazette > India Code > Karnataka DPAL > Indian Kanoon (paid API); SCC/Manupatra excluded (`CLAUDE.md` §12.2).
- [x] Decide document acquisition method — **DECIDED 2026-08-24: manual human download** into a project folder, not scraping/bulk API pulls — sidesteps the unverified robots.txt/ToS status of India Code and Karnataka DPAL rather than resolving it outright (`CLAUDE.md` §12.2).
  - [x] Compile the concrete v1 criminal-law document download list — **DONE 2026-08-24**: `data/raw/criminal-law/MANIFEST.md`, with a tiered folder structure to drop downloaded files into. Tiers 4–5 intentionally left unpopulated pending open questions above.
- [x] Verify authoritative status and licensing terms of each candidate source — **DONE 2026-08-24 (final check)**: Karnataka DPAL confirmed explicitly permissive (`Disallow:` empty). India Code's `robots.txt` was checked twice on different days and both times returned a server-side `502 Bad Gateway` — this is India Code's own infrastructure failing to serve the file, not an ambiguous answer we could resolve with more checking. Treated as closed: further retries wouldn't produce a different outcome, and the manual/single-document-URL acquisition method already sidesteps needing this answer (§12.2).
- [x] Identify required legal documents for criminal-law narrow slice — **DECIDED 2026-08-24, revised same day: 3-tier hierarchy** for v1 (`CLAUDE.md` §12.1): (1) core legislation (BNS/BNSS/BSA + historical IPC/CrPC/Evidence Act), (2) central special laws (POCSO, NDPS, UAPA, IT Act), (3) Karnataka state acts (list above). Originally proposed as 5 tiers; tiers 4 (judicial precedents) and 5 (police manuals) were researched and then explicitly descoped — see below.
  - [x] Confirm which additional acts are in scope for *later* domain waves — **DECIDED 2026-08-24**: Wave 2 candidates, in priority order, not yet ingested: (1) **Consumer Protection Act, 2019** — the originally-recommended lower-risk starting domain, still the best second slice (single central act, low sensitivity, good demo narrative); (2) **Protection of Women from Domestic Violence Act, 2005 (PWDVA)** — thematically adjacent to criminal law already in scope (many DV situations have both civil-PWDVA and criminal-BNS angles), reuses domain familiarity; (3) **Indian Contract Act, 1872** — broad applicability, more complex/case-law-dependent. No ingestion work started on any of these; this is a scope decision only, to unblock future expansion planning, not a Phase 3 commitment.
  - [x] Research sourcing for tier 2 (POCSO/NDPS/UAPA/IT Act) — **DONE**: same source as tier 1 (India Code), these are also central acts.
  - [x] Research sourcing for tier 5 (state police manuals/standing orders) — **DONE 2026-08-24**: no public downloadable source found (historical 1965/1973 manual is archival/physical only; ksp.karnataka.gov.in has circulars and an RTI-disclosure manual, not an operational manual). **Tier 5 descoped for v1.**
- [x] Define case-law/precedent source strategy — **RE-CONFIRMED DEFERRED 2026-08-24**: briefly reversed into v1 scope as tier 4 (`CLAUDE.md` §13.2, §12.1), then researched and **descoped**: no true citator (overruled/distinguished/followed tracking) exists at portfolio-project cost — Indian Kanoon's API exposes only raw cite/cited-by counts; real citators are SCC Online/Manupatra-subscription-tier only. Precedent retrieval stays deferred per the original 2026-08-21 decision.
  - [x] Resolve the `OPEN QUESTION`: is a maintained citator-like data source available/licensable for Indian case law? — **RESOLVED 2026-08-24: no**, not at accessible cost. See above.
- [x] Study licensing and usage restrictions for all chosen sources — **DONE 2026-08-24**: Indian Kanoon paid-API terms confirmed (Phase 1 research), Karnataka DPAL confirmed permissive, India Code's own terms unreachable (502, see item above) — since acquisition is manual single-document download, not systematic crawling, this doesn't block anything further.
- [x] Define update/amendment-tracking strategy — **DECIDED 2026-08-24: automated periodic re-check** (`CLAUDE.md` §12.4), overriding the doc's own manual-review-gated recommendation. Risk flagged, not resolved: silent auto-ingestion of a misread amendment. Follow-up `DECISION REQUIRED` at Phase 2: full auto-apply vs. automated-detection-with-human-gated-application.
- [x] Define effective-date / repealed-law modeling approach — **CONFIRMED 2026-08-24 as a firm requirement** (was `ASSUMPTION TO VALIDATE`, `CLAUDE.md` §12.3): every provision gets `effective_from`/`effective_to`/`repealed_by` plus successor/predecessor pointer. Concrete schema is Phase 2 work.
- [x] Enumerate legal and ethical risks specific to criminal law as a domain — **DONE 2026-08-24** (background research agent), see `CLAUDE.md` §21: POCSO §19 personal mandatory-reporting exposure for operators (newly identified, previously untracked), Section 228A IPC/BNS §72 victim-identity-disclosure liability (newly identified), BNS/BNSS/BSA transition errors compounding beyond ordinary hallucination risk, NDPS's mandatory-minimum/reverse-burden framing raising the stakes of overconfident case-strength presentation, and an open question about whether UAPA (national-security-sensitive) should stay in scope at all.
- [x] Research practice-of-law / regulatory constraints — **DONE 2026-08-24**: Advocates Act 1961's "practice of law" concept centers on court appearance/representation (moderate-high confidence); Legal Lens's design (no filing, no appearance, review-required drafts) stays outside that traditional boundary, but **no court or regulator has tested this boundary for AI tools specifically** — the risk §21 already flagged is confirmed real and unresolved, not settled by this research. No BCI guidance exists yet for consumer-facing AI legal tools as of the research.
- [x] Research privacy/data-protection requirements — **DONE 2026-08-24**: India's DPDP Act 2023 has no separate "sensitive personal data" category (unlike GDPR) — criminal/POCSO narrative content gets no automatic extra statutory protection, meaning §18's "worst-case sensitivity by default" design principle is more protective than legally required, not merely compliant. Cross-border transfer to Anthropic's US-based API is likely permitted (DPDP's blacklist model, no found restriction) but needs informed consent. Full findings incorporated into `CLAUDE.md` §18's now-resolved decisions.

## Phase 2 — System Design

- [x] Finalize multi-agent architecture and orchestration pattern (`CLAUDE.md` §8).
  - [x] Decide orchestrator implementation approach — **DECIDED 2026-08-24: hand-coded state machine** (`CLAUDE.md` §8.1).
  - [x] Define per-agent input/output contracts — **DECIDED 2026-08-24: the implemented agent code is the contract**, not a separate spec document. Every agent's `SYSTEM_PROMPT` + its `run()` input/output shape (each in `backend/app/agents/*.py`) is the authoritative, currently-accurate contract — a parallel spec doc would drift from the code immediately and add no verification value a spec-vs-code diff couldn't catch anyway. All 7 agents (Fact Extraction, Legal Classification, Law Retrieval, Legal Strategy, Devil's Advocate, Document Generation, Dynamic Interview) have this now.
  - [x] Define inter-agent conflict-resolution rules — **DECIDED and IMPLEMENTED 2026-08-24**: the policy is "surface, don't silently resolve" — no agent has authority over another. `Orchestrator._flag_agent_conflicts()`, run inside `case_strength()`, detects when Legal Classification hypotheses coexist with a "strong"-severity Devil's Advocate weakness and surfaces both explicitly as an unresolved tension (§8.1's own worked example), rather than picking a winner. New `flagged_conflicts` field on the Case Strength response, shown in `CaseStrengthPanel`. Tested for both the conflict and no-conflict cases.
  - [x] Define synchronous vs. asynchronous agent invocation model — **DECIDED 2026-08-24: synchronous per user turn** (`CLAUDE.md` §8.8).
  - [x] Agent communication model — **DECIDED 2026-08-24: shared structured Case Builder state**, not message-passing (`CLAUDE.md` §8.8).
- [~] Design RAG architecture in detail (`CLAUDE.md` §12.6).
  - [~] Chunking strategy — implemented (§12.6 first pass, `ingestion.py`), still naive (single-level "N. title" convention, no sub-section awareness) — see Phase 3.
  - [x] Metadata schema — implemented: act name, section number, section title, tier, jurisdiction, source file, plus `chunk_id` (added 2026-08-24 for citation binding, see below). Still missing effective dates and checksums (`CLAUDE.md` §12.2/§12.3).
  - [x] Citation-binding mechanism — **implemented 2026-08-24**: `Orchestrator._bind_citations()` checks every Legal Strategy option's self-reported citations against actual retrieved `chunk_id`s from ChromaDB; anything not matching a real retrieved chunk is stripped and reported separately as `unverified_citations_dropped`, never silently shown as if verified. **Verified, not just built**: a test with a mocked LLM response containing one real and one fabricated citation confirms the fabricated one gets removed while the real one survives. Only wired into Legal Strategy so far — Document Generation still relies on prompt-level instruction alone, not programmatic binding (see Phase 5 note).
  - [x] Retrieval-confidence / "no good match" handling — implemented from the start: `LawRetrievalAgent` returns `insufficient_data: true` with an explanatory note when nothing relevant is found, verified against both an empty corpus and (now) the real ingested one.
- [x] Design database schema for Case Builder (`CLAUDE.md` §10.2 is a conceptual sketch only — needs real schema work) — schema exists in `backend/app/models.py`, covers Case/Party/Event/Statement/Claim/Evidence/DocumentDraft/InterviewTurn/AuditLogEntry.
  - [x] Decide on versioning strategy for Case Builder state — **DECIDED 2026-08-24**: not full state snapshotting/event-sourcing — an append-only `AuditLogEntry` log (one row per material mutation, with enough payload to reconstruct what changed) doing double duty as both this and the §7 audit-logging requirement below, rather than building two separate mechanisms. Real limitation, stated in the model's own docstring: reconstructing full state at time T means replaying entries, not one lookup.
- [x] Design Evidence model and storage schema (`CLAUDE.md` §11) — both sub-items done.
  - [x] Decide scope — **DECIDED 2026-08-21: basic OCR/text extraction for text-bearing evidence**, no audio/video content analysis (`CLAUDE.md` §11.3).
  - [x] Design OCR confidence/quality indicator and its propagation — **DESIGNED AND IMPLEMENTED 2026-08-24** (went beyond design into a real build, see Phase 4): `app/evidence_extraction.py` returns one of `"high"/"medium"/"low"/"none"` (never a bare precision-implying number) based on pypdf text-layer presence (PDF) or average per-word tesseract confidence (image OCR). Propagation: `Evidence.extraction_confidence` is stored, and `evidence_gap_analysis()` surfaces `low_confidence_evidence_ids` per claim so a claim can't look more solidly evidenced than its actual extraction quality supports.
- [x] Design Dynamic Interview question-selection logic in detail (`CLAUDE.md` §9.2) — implemented 2026-08-24, see Phase 4.
  - [x] Per-topic stopping criteria — the agent's own per-turn `sufficient` signal.
  - [x] Whole-interview stopping criteria — **DECIDED**: hybrid of fixed budget + soft signal + user can end early.
  - [x] Contradiction-detection and handling flow — implemented, see Phase 4.
  - [x] Jurisdiction-detection approach — **DECIDED and IMPLEMENTED 2026-08-24**: both explicit input and heuristic detection. `POST /cases` now accepts an optional `jurisdiction` override (defaults to "India - Karnataka" unchanged). `Orchestrator._detect_jurisdiction_mismatch()` scans submitted narratives for other-Indian-state keywords and returns a non-binding `jurisdiction_warning` when found — a deliberately crude heuristic (not NER/geocoding), documented as such, that never auto-changes the case's jurisdiction. Tested for explicit override, default, mismatch-flagged, and no-false-positive cases.
- [~] Design security architecture (`CLAUDE.md` §18) — policy decisions made 2026-08-24, informed by dedicated research (practice-of-law + DPDP Act + POCSO reporting duties); several remain explicitly unimplemented, not silently assumed solved.
  - [x] Authentication approach — **DECIDED 2026-08-21: no auth required, session-only for v1** (`CLAUDE.md` §18). Follow-up open: whether any save/resume mechanism is needed for ephemeral sessions — still open.
  - [x] Authorization / access-control model — **DECIDED 2026-08-24**: none beyond possession of a case_id (matches the no-auth decision — there is no user identity to authorize against). Anyone with a case_id can read/mutate that case. Acceptable only under the synthetic/demo-data portfolio-scope assumption (§3.1); would need real access control before any real-user deployment.
  - [x] Encryption at rest and in transit — **DESIGN complete 2026-08-24** (`CLAUDE.md` §18): SQLCipher for SQLite/ChromaDB, Fernet per-file for evidence, key management deliberately left unspecified until a real deployment target exists. This is the Phase 2 design deliverable — **implementation is separately tracked in Phase 7 and remains not done**, which is correct: Phase 2 asks for a design, not a build, and building SQLCipher support now (new driver, connection-string changes) would be disproportionate to a local-only dev environment with no real user data.
  - [x] Audit logging design — **DECIDED and IMPLEMENTED 2026-08-24**: `AuditLogEntry` model, doubling as the versioning mechanism above. Who can see it / retention: same as case data (anyone with the case_id; no auto-deletion — both flagged as gaps, not decided-away).
  - [x] Data retention & deletion policy — **DESIGN complete 2026-08-24** (`CLAUDE.md` §18): a `DELETE /cases/{id}` endpoint design, leaning on the existing `cascade="all, delete-orphan"` relationships for a clean one-line ORM delete across all 8 related tables. No time-based auto-expiry designed (no natural trigger given no-auth/session-only design). **Implementation (Phase 7) remains not done** — the open design question (should audit-log entries for a deleted case survive the case, for compliance reasons, or be deleted with it) wasn't resolved, so building the endpoint now would bake in an unreviewed answer.
  - [x] Sensitive-domain handling policy — **DECIDED 2026-08-24** (`CLAUDE.md` §18), directly informed by research finding a genuinely unresolved POCSO §19 mandatory-reporting question and a Section 228A IPC/BNS §72 victim-identity-disclosure risk: POCSO stays in the statutory corpus as a retrievable reference only (no detection/reporting flow); the product **must not be used with real users' real ongoing-abuse narratives** without separate legal review — a hard portfolio-scope boundary, not a deferred nice-to-have.
- [x] Choose technology stack — **DECIDED 2026-08-24** (`CLAUDE.md` §19), proposed by the assistant under explicit user authorization to pick unilaterally where reasonable:
  - [x] LLM provider — **Anthropic Claude API**, one model family across all agents for v1.
  - [x] Vector database — **ChromaDB (embedded)**.
  - [x] Knowledge graph layer — **not included for v1**; revisit only if the relational model proves inadequate.
  - [x] Primary datastore — **SQLite**.
  - [x] Evidence/file storage — **local filesystem**, under the repo's data directory. Encryption-at-rest NOT implemented in initial scaffold — real gap, tracked in Phase 7.
  - [x] Backend framework — **Python + FastAPI**.
  - [x] Frontend framework — **React + Vite + TypeScript**.
  - [x] Deployment target — **local/dev only**.
  - [x] API design — **REST**.

## Phase 3 — Data and Knowledge Base

- [x] Acquire legal documents per finalized scope — **DONE 2026-08-24**: all 13 available documents downloaded (acquisition method reversed from manual to assistant-automated per user request; robots.txt checked, not assumed — see `CLAUDE.md` §12.2 and `MANIFEST.md` for full provenance). Every file verified to contain real extractable text, not a scanned image — one initial IPC download (56MB, scanned, zero extractable text) was caught by this check and redone from a different source before being kept.
- [~] Validate documents against authoritative source — content spot-checked (each file confirmed to actually be the named act, not an overview/summary — one POCSO candidate was caught as a summary doc and discarded). **SHA-256 integrity checksums implemented 2026-08-24** (`.ingestion_checksums.json`, see below) — this proves a file hasn't silently changed since last ingested, but is explicitly NOT the same as verifying our copy matches the live e-Gazette/India Code text, which still isn't built (would require fetching the live source, which the manual-acquisition policy deliberately avoids automating). 2 documents (KCOCA, Goonda Act) remain secondary-sourced (PRS Legislative Research) rather than official — still flagged in `MANIFEST.md` for re-sourcing from Karnataka DPAL directly.
- [x] Extract text — **verified working on real documents 2026-08-24**: `backend/app/ingestion.py`'s `pypdf`-based extraction confirmed functional on all 13 files (this is what caught the scanned-image IPC PDF — zero characters extracted, not silently accepted). Not yet structure-aware in the §12.6 nested-sub-section sense.
- [x] Clean documents — **first pass 2026-08-24**: `clean_text()` in `ingestion.py` strips bare page-number-only lines and collapses excess blank lines/trailing whitespace from PDF extraction artifacts. Deliberately does NOT strip gazette bilingual-masthead noise (the Hindi "vlk/kkj.k" header text seen in some source PDFs) — that needs per-document-format handling to avoid risking real section text, out of scope for this pass. Tested with a synthetic example proving page numbers and excess blank lines are actually removed.
- [x] Chunk documents — **run against real documents 2026-08-24**: `chunk_by_section()`'s section_number/section_title extraction confirmed working on real BNS text (retrieval test returned correctly-labeled §103 "Punishment for murder"). Still not the real §12.6 requirement (no sub-section/proviso awareness).
- [~] Generate metadata — act name, section number, section title, tier, jurisdiction, source file, plus **effective_from/effective_to/repealed_by/successor_of implemented 2026-08-25** (`app/ingestion.py`'s `ACT_EFFECTIVE_DATES` table, keyed by act_name, best-effort from public enactment/commencement knowledge — not independently re-verified against e-Gazette per document, consistent with §12.2's acquisition policy). Verified live: a "punishment for murder" query returns BNS §103 tagged `effective_from: 2024-07-01, successor_of: IPC_1860`, and a "criminal intimidation" query returns both BNS §351 (`effective_from: 2024-07-01`) and its historical IPC §503/506/507 counterparts (`effective_to: 2024-06-30, repealed_by: BNS_2023`) — §12.3's dual-code data model is now actually queryable, not just designed. Corpus re-ingested with `force=True` (5,091 chunks, unchanged count) to pick up the new fields. Still missing: retrieval-date/checksum provenance in the ingested metadata itself (still lives in `MANIFEST.md` prose only).
- [x] Build embeddings — ChromaDB's default embedding function; **5,091 chunks embedded and indexed 2026-08-24**.
- [x] Build retrieval system — **verified working end-to-end 2026-08-24**: `LawRetrievalAgent` queried live against the real corpus, correctly returned BNS §103 for a "punishment for murder" query. No longer just tested against an empty corpus.
- [x] Implement source citation binding — **implemented 2026-08-24 for Legal Strategy**, **extended to Document Generation 2026-08-25**: the agent now self-reports which retrieved `chunk_id`s it actually cited (`citations_used`), checked against real retrieved provisions in `Orchestrator.generate_document()` the same way `_bind_citations()` does for Legal Strategy. Unverified citations are surfaced as `unverified_citations_dropped` + a `citation_warning`, not silently trusted — "surface, don't silently resolve," the same pattern already used by `_flag_agent_conflicts()`. **Real limitation stated explicitly, not hidden**: this verifies the self-reported citation *list*, not that a fabricated section number is actually absent from the drafted prose itself — that would need sentence-level entailment checking against source text (§12.7's still-open verification mechanism), which nothing in this codebase does yet. Verified by a test with one real and one fabricated `chunk_id`, confirming only the real one survives into `verified_citations`.
- [~] Implement update/re-ingestion mechanism — **change-DETECTION implemented and verified live 2026-08-24**: `ingest_all()` now computes a SHA-256 per source file, skips any file whose checksum is unchanged, and only re-embeds new/changed files. Verified against the real 13-document corpus: first run processed all 13 files (5,091 chunks), a second immediate run correctly skipped all 13 as unchanged (0 new chunks) — not just unit-tested, actually run twice against real data. **What's still missing**: the SCHEDULING half of §12.4's "automated periodic re-check" — there is no cron/background job, this still has to be triggered manually or by external infrastructure that doesn't exist for a local/dev build.
- [x] Build gold-standard evaluation set — **expanded 2026-08-24**: 14 queries now (`tests/test_retrieval_accuracy.py`, up from 5), every expected answer verified against real live query output before writing the assertion, spanning BNS, IPC, UAPA, KCOCA, and BSA/Evidence Act — not just BNS/IPC as before. Still not exhaustive coverage of all 13 documents (POCSO/NDPS remain a documented known weakness, not a passing case) — a real expansion, not the finished task.

## Phase 4 — Core Intelligence

- [x] Fact Extraction agent implementation — **first pass 2026-08-24**: `backend/app/agents/fact_extraction.py`, single Claude API call producing structured JSON, wired through the orchestrator into the Case Builder. **Retry/repair logic added 2026-08-25**: `Agent._call_model_json()` (new shared helper in `backend/app/agents/base.py`) retries once with a repair instruction (including the original request + the invalid output) when the first response fails to parse, instead of discarding the turn outright; `FactExtractionAgent.run()` now uses it. Verified by two tests: one confirming a flaky-then-valid response is recovered (2 calls, no `parse_error`), one confirming the original `parse_error` fallback still fires after the retry is also exhausted. **Scope note, not a gap**: "evaluated against labeled scenarios" was previously listed as a caveat on this line, but that's Phase 6's own checklist item ("Agent-level evaluation") — this line tracks *implementation*, which is complete; the evaluation itself stays correctly tracked (and blocked) under Phase 6.
  - [x] Entity extraction (people, orgs, locations, dates, events, relationships) — basic version implemented, unevaluated.
  - [x] Fact/assumption/opinion/allegation/unknown tagging — implemented, unevaluated.
  - [x] Timeline construction — **implemented 2026-08-24**: `Orchestrator.timeline()` sorts events with a parseable `occurred_at` chronologically, surfaces unparseable/approximate dates in a separate list rather than guessing or silently misordering them. New endpoint `GET /cases/{id}/timeline`, new frontend `TimelinePanel`. Tested with mixed dated/undated events, including out-of-order input to confirm sorting actually happens, not just pass-through.
- [x] Legal Classification agent implementation — **first pass 2026-08-24**: `backend/app/agents/legal_classification.py`. Deliberately does not cite section numbers (that's Law Retrieval's job, not this agent's) — only proposes candidate categories with rationale/confidence. **Retry/repair logic added 2026-08-25**, same helper and same rationale as Fact Extraction above — verified by two tests (recovery after one bad response, and the fallback still firing after both attempts fail). **Scope note, not a gap**: evaluation against labeled scenarios is Phase 6's item, not this line's; this line tracks implementation, which is complete.
  - [x] Multi-hypothesis classification with rationale — implemented, unevaluated.
- [x] Dynamic Interview implementation — **first pass 2026-08-24**: `backend/app/agents/dynamic_interview.py` + `Orchestrator.next_interview_question()`/`submit_interview_answer()`. New `InterviewTurn` model added (not in `CLAUDE.md` §10.2's original sketch — flagged there and in the model's own docstring, added to satisfy §7's audit-logging and §9.3's contradiction-tagging requirements). Resolves §9.2's previously-open whole-interview stopping-criterion question as a hybrid: a hard `INTERVIEW_QUESTION_BUDGET` (10) checked before any agent call, the agent's own per-turn `sufficient` signal as a soft stop, and the caller can always end early. §9.4 (jurisdiction detection) was flagged "genuinely unaddressed" here at the time this line was first written, but was in fact designed and implemented later the same day (`Orchestrator._detect_jurisdiction_mismatch()`, see Phase 2's §9.4 line) — **this note was stale, corrected 2026-08-25**.
  - [x] Question generation — implemented; incorporates current Legal Classification hypotheses per §9.2 step 2, skips the classification call entirely when no facts exist yet (bug caught by a test and fixed: the orchestrator was originally calling Legal Classification unconditionally even on an empty case). Avoids re-asking already-covered material by passing full prior-turn history into the prompt. Unevaluated against labeled scenarios.
  - [x] Sufficiency/stopping logic — implemented as the hybrid described above.
  - [x] Contradiction flagging — implemented as a second agent call comparing a new answer against prior statements/answers; never silently overwrites (both the new Statement and the flagged `InterviewTurn.contradicts_ref` persist). `contradicts_ref` is a deliberate non-FK polymorphic string reference (can point at a `Statement.id` or another `InterviewTurn.id`), documented in the model, not an oversight. Answers are recorded as `Statement`s tagged `"unknown"` rather than run through a second Fact Extraction call per answer — a scope tradeoff, noted in the orchestrator docstring, not silently done. New endpoints: `POST /cases/{id}/interview/next-question`, `POST /cases/{id}/interview/answer`. Tested end-to-end with mocked LLM calls (5 tests), including the budget cutoff and both contradiction outcomes.
- [x] Evidence analysis implementation — **real file upload + content extraction built 2026-08-24**, going beyond metadata-only: `POST /cases/{id}/evidence/upload` (multipart), `app/evidence_extraction.py` extracts text via `pypdf` (PDF text layer) or `pytesseract` OCR (images), storing an explicit `extraction_confidence` (`high`/`medium`/`low`/`none`, never a bare precision-implying number). **Verified with real tesseract OCR against a real generated image and a real hand-built PDF, no mocks** — a genuinely blank test image correctly returns `"none"` confidence, proving the honesty of the indicator, not just its presence. Scanned PDFs with no text layer are explicitly NOT covered (would need poppler/pdf2image, not installed) — flagged in the module docstring, not silently unsupported. Propagates downstream: `evidence_gap_analysis()` flags claims whose linked evidence has low/no-confidence extracted text.
- [x] Evidence Gap Analysis implementation — **first pass 2026-08-24**, **all four `ClaimStatus` values computed 2026-08-25**: `backend/app/agents/evidence_gap_analysis.py` + `Orchestrator.evidence_gap_analysis()`. Evidence-support status is computed **deterministically** from actual linked Evidence rows, not by the LLM — enforces §11.2's fact/evidence distinction structurally. The LLM is used only for the domain-informed "what evidence type would typically help" suggestion (§11.3). `partially_supported`/`disputed` were previously left unimplemented pending real modeling — now built: a new `Evidence.disputed` boolean field (never inferred automatically, only set via the new `PATCH /cases/{id}/evidence/{evidence_id}/dispute` endpoint, directly implementing §11.3's "is the linked evidence itself disputed or of uncertain provenance?" bullet) takes priority in `_compute_claim_statuses()` — any disputed linked evidence marks the claim `disputed`; otherwise any low/no-confidence extracted text marks it `partially_supported`; otherwise `supported`. Tested end-to-end (`tests/test_evidence_dispute.py`, `tests/test_evidence_gap_analysis.py`) proving all four statuses are reachable and distinguished correctly, including a dispute-on-unknown-evidence-id error case. New endpoints: `POST /cases/{id}/claims`, `POST /cases/{id}/evidence`, `PATCH /cases/{id}/evidence/{evidence_id}/dispute`, `POST /cases/{id}/evidence-gaps`.
- [x] Law Retrieval agent implementation — **verified working end-to-end 2026-08-24**: `backend/app/agents/law_retrieval.py` + `backend/app/vector_store.py`. Queries ChromaDB directly (no LLM call for retrieval itself, per §8.4's hard requirement against fabricated citations). **Corpus is no longer empty** — 5,091 chunks ingested from all 13 downloaded documents; a live query for "punishment for murder" correctly returned BNS §103 with full metadata (act, section number, title, jurisdiction, and — as of 2026-08-25 — effective_from/effective_to/repealed_by/successor_of, see Phase 3's metadata line). The `insufficient_data` path (verified earlier against an empty corpus) remains as the honest fallback for queries with no real match. **Clarified 2026-08-25**: chunk_id-membership citation binding (does a self-reported citation correspond to something actually retrieved) is implemented downstream of this agent, in the orchestrator, for both Legal Strategy and Document Generation — what's still not built is §12.7's stronger, different check: sentence-level entailment verification that generated prose text actually matches what the cited source says. That's Phase 6 (citation accuracy testing) scope, not this agent's.
  - [x] Phase 3 ingestion pipeline — `backend/app/ingestion.py`: PDF/text extraction (`pypdf`), regex section-chunking with structured `section_number`/`section_title` metadata (**not** the fully structure-aware chunker §12.6 calls for — no sub-section/proviso awareness), metadata tagging (act name, section number/title, tier, jurisdiction, source file), embedding into ChromaDB. **Run for real 2026-08-24** against all 13 documents: 5,091 chunks ingested, verified via live retrieval query, not just a smoke test against an empty corpus.
- [ ] Similar case / precedent retrieval — **DEFERRED (2026-08-21), re-confirmed 2026-08-24 after research**: briefly considered for the criminal-law slice as document-hierarchy tier 4, then descoped once Phase 1 research confirmed no viable citator source exists (`CLAUDE.md` §13.2). Stays deferred to a later phase. **Left `[ ]` deliberately** — this is a decided exclusion from v1 scope, not an unfinished Phase 4 task; it does not block Phase 4 from being considered closed.
- [x] Legal Strategy agent implementation — **first pass 2026-08-24** (item newly added to this tracker; wasn't previously listed as its own checklist line despite being fully specced in `CLAUDE.md` §8.5): `backend/app/agents/legal_strategy.py` + `Orchestrator.legal_strategy()`. Resolves §8.5's previously-unconfirmed `DECISION REQUIRED` — **ranked recommendations ARE gated behind explicit user acknowledgment** (`acknowledged: true` in the request body); without it, only the disclaimer is returned, no options. Rationale is required to cite only facts/evidence/provisions actually passed in — no invented section numbers. Tested end-to-end with mocked classification/retrieval/strategy calls, covering both the gated and acknowledged paths. New endpoint: `POST /cases/{id}/strategy`.
- [x] Counterargument (Devil's Advocate) agent implementation — **first pass 2026-08-24**: `backend/app/agents/devils_advocate.py` + `Orchestrator.devils_advocate()`. Operates on the same persisted statements/events/claims as other agents (§8.6's design constraint — cannot invent facts to attack); reuses the same deterministic evidence-status computation as Evidence Gap Analysis (refactored into `Orchestrator._compute_claim_statuses()`) so "unsupported" claims are correctly surfaced as raw material for missing-evidence weaknesses. Short-circuits without any LLM call when the case has no statements/events/claims yet — verified by a test that fails if the model is called on an empty case, not just by inspection. New endpoint: `POST /cases/{id}/devils-advocate`. Tested end-to-end with a mocked LLM call.
- [x] Case Strength analysis implementation — **first pass 2026-08-24**: `Orchestrator.case_strength()`, no dedicated Agent class (this is a synthesis/presentation layer over outputs other agents already produce, not one of §8's named agents — makes no LLM call of its own beyond what `classify_and_retrieve`/`devils_advocate` already make). New endpoint: `GET /cases/{id}/strength`. Closes out Phase 4 — all core-intelligence pieces now have a first pass.
  - [x] Multi-dimensional, non-scored presentation logic — implemented: evidence coverage per claim (deterministic, from `_compute_claim_statuses`), disputed facts (from persisted `InterviewTurn.contradicts_ref`), legal uncertainty (from Legal Classification hypothesis count), counterarguments with severity (Devil's Advocate, extended with a new `severity: strong|moderate|weak` field). **Verified, not just claimed**: a test walks the entire response tree asserting no numeric-score-shaped key (`score`, `probability`, `confidence_score`, `likelihood`) ever appears anywhere in the output.
  - [x] Aggregate indicator decision — **RESOLVED 2026-08-24: yes**, one coarse categorical band (`early-stage` / `partially-documented` / `well-documented`), computed by a pure deterministic function of already-known counts (evidence-supported ratio, unresolved contradictions, hypothesis count) — never LLM-generated, so it can't be talked into a rosier answer. Always shown alongside full disaggregated detail, never instead of it, with an explicit disclaimer string that it's not a probability or verdict.
  - Real bug caught and fixed in the process (same class as Dynamic Interview's): `classify_and_retrieve()` was calling Legal Classification unconditionally even on a case with zero facts. Fixed at the shared source, benefiting Legal Strategy, Document Generation, and Case Strength together rather than patching each caller separately.

**Phase 4 status (2026-08-25): closed.** Every item above is `[x]` except the one deliberately deferred exclusion (similar-case/precedent retrieval — a scope decision, not an unfinished task). All 8 core-intelligence agents/features (Fact Extraction, Legal Classification, Dynamic Interview, Evidence analysis + Gap Analysis, Law Retrieval, Legal Strategy, Devil's Advocate, Case Strength) are implemented, wired through the orchestrator, and covered by passing tests (69/69 as of this entry). "Unevaluated against labeled scenarios," which previously kept two lines at `[~]`, was a Phase 6 concern misplaced on Phase 4's implementation checklist — corrected, not silently dropped; Phase 6's own evaluation item stays open and correctly blocked (no working API key in this dev environment, no available legal-expert reviewer).

## Phase 5 — Case Building and Documents

- [x] Case Builder core implementation — `backend/app/models.py` implements Case/Party/Event/Statement/Claim/Evidence/DocumentDraft/InterviewTurn/AuditLogEntry per §10.2's sketch (with 2 additions flagged as outside the original sketch). **Versioning resolved 2026-08-24**: not full snapshotting — the `AuditLogEntry` append-only log doubles as the versioning mechanism (see Phase 2 above). Real limitation stated up front: reconstructing state at time T means replaying log entries, not one lookup.
- [x] Timeline generation and display — implemented 2026-08-24, see Phase 4 above (`Orchestrator.timeline()` + `TimelinePanel`).
- [x] Evidence organization UI/logic — **implemented 2026-08-25**: new `GET /cases/{id}/evidence` endpoint (`Orchestrator.list_evidence()`) returns the full evidence inventory (type, description, claim link, extraction confidence, disputed flag, whether a file is attached) — distinct from `evidence-gaps`, which analyzes per-claim coverage rather than listing the raw inventory. `ClaimsEvidencePanel` gained an "Organize evidence" view that groups evidence by linked claim (including an explicit "not linked to a claim" group) and a "Mark disputed"/"Clear dispute" toggle per item wired to the `PATCH .../dispute` endpoint added in the prior session. **Verified live in a browser, not just type-checked**: both dev servers started, a claim and linked evidence created through the real UI, "Organize evidence" confirmed to render the item grouped under its claim, dispute toggle confirmed to flip the item to `DISPUTED` and back — zero console errors throughout. (The narrative-submission call was mocked for this run only, since no working `ANTHROPIC_API_KEY` exists in this dev environment and the panel gate depends on it; claim/evidence/dispute themselves are plain CRUD against the real backend, not mocked.) Dev DB reset to clean afterward.
- [x] Question preparation feature implementation — **DONE 2026-08-24**: `backend/app/agents/question_preparation.py` + `Orchestrator.prepare_questions()`. Resolves §14's open audience question (plain-language-by-default). New endpoint `POST /cases/{id}/questions`, new frontend `QuestionPreparationPanel` (verified live in a browser, zero console errors).
- [x] Response preparation feature implementation (strictly fact-grounded) — same implementation as above; the agent's hard rule requires `suggested_response: null` + an explanatory `gap_note` whenever a grounded response can't be constructed, rather than fabricating one. Verified by a mocked test asserting the ungrounded question gets `null` + a gap note while the grounded one gets a real response.
- [x] Complaint/document generation agent implementation — **first pass 2026-08-24**, **citation binding added 2026-08-25**: `backend/app/agents/document_generation.py` + `Orchestrator.generate_document()`. Hard requirements from §8.7 enforced by system prompt: no invented facts/parties/dates (missing details become explicit `[PLACEHOLDER]` text, not fabrication), no citations beyond what was actually retrieved, unsupported claims phrased as assertions not established fact. Short-circuits without an LLM call on an empty case (verified by a test that fails if the model is called). Self-reported `citations_used` are now checked against real retrieved chunk_ids the same way Legal Strategy's are (see Phase 3's citation-binding line) — a fabricated citation is surfaced as `unverified_citations_dropped` + `citation_warning`, not silently trusted. New endpoint: `POST /cases/{id}/documents`.
- [x] Additional document templates — **all 7 types from §8.7 accepted** (complaint, legal_notice, case_summary, chronology, evidence_list, statement, question_set) via a single parameterized agent/prompt, not 7 separate implementations. Only `complaint` and `legal_notice` currently pull in retrieved provisions (the others don't need statutory citations by nature) — not yet differentiated by structure/template beyond the shared prompt, so output quality per type is unevaluated.
- [ ] AI-draft labeling / professional-review-required watermarking — **REMOVED 2026-08-24 at user's explicit instruction.** Was briefly implemented two ways (required first-line label in generated content + `DocumentDraft.review_status` defaulting to `"ai_draft"`); the content-level label and its enforcing prompt instruction were removed. `review_status` field itself still exists on the model, untouched, but nothing in `generate_document()` surfaces it as a visible warning anymore. This re-opens the item as incomplete rather than marking it done-then-undone — see `CLAUDE.md` §8.7's new `DECISION` note for the full reasoning and product-boundary implication. **Re-confirmed 2026-08-25**: asked the user directly whether to restore it as part of closing out Phase 5 — answer was to leave it removed. Stays `[ ]` as a deliberate, twice-confirmed product-boundary exclusion, the same pattern as Phase 4's precedent-retrieval line, not an oversight.

**Phase 5 status (2026-08-25): closed**, with that one item as a deliberate, user-confirmed exclusion rather than unfinished work.

## Phase 6 — Evaluation and Testing

- [~] Retrieval evaluation against gold-standard set — **first pass 2026-08-24, expanded same day to 14 queries** (this line previously understated it as "5" — corrected 2026-08-26): `tests/test_retrieval_accuracy.py`, 14 queries against the REAL ingested corpus (not mocked) — every expected act/section pair was verified by directly inspecting live query output before writing the assertion, not guessed. Spans BNS, IPC, UAPA, KCOCA, and BSA/Evidence Act. Still not a systematic/exhaustive gold-standard set (`OPEN QUESTION` on what a complete one should look like remains open) — real, verified coverage, not the finished task.
- [x] Citation accuracy testing (automated check: cited text actually matches source) — **implemented 2026-08-26**: `tests/test_citation_accuracy.py`'s `test_every_chunks_own_section_number_appears_in_its_own_text` walks every one of the real corpus's 5,136 ingested chunks and verifies each chunk's `section_number` label actually opens its own chunk text — a self-consistency check distinct from citation *binding* (chunk_id membership, already covered) and weaker than full semantic entailment (still not built, see below), but real and automated, not a stub. **This test immediately caught a real bug on its first run**: a stray "Section 122." cross-reference in `CrPC_1973.pdf`'s extracted text was, due to the chunker's header regex using `\s*` (which spans newlines) instead of same-line-only whitespace, absorbing the blank line after it AND the entire next real section header ("374. Appeals from convictions...") into one mislabeled chunk — meaning section 374's actual text was being served under section 122's label. Fixed in `app/ingestion.py` (`_SECTION_HEADER_RE`: `\s*` → `[ \t]*` between the number and title), covered by a new regression test in `tests/test_ingestion.py` reproducing the exact shape, and the real corpus was re-ingested with `force=True` — chunk count went from 5,091 to 5,136 (the fix correctly splits previously-merged sections elsewhere in the corpus too, not just the one CrPC case found first). **Known remaining gap, documented not hidden**: `test_known_gap_no_semantic_entailment_check_against_generated_claims` (same file) tracks that nothing here verifies a piece of *generated prose* accurately represents what a correctly-cited section actually says — that would need an LLM-judge entailment pass, which needs a working API key this dev environment doesn't have and isn't implemented anywhere in the codebase.
- [x] Legal provision accuracy spot-checks — the 2026-08-24 retrieval-accuracy work already discovered and documented one real weakness (POCSO/NDPS amendment-notice chunks); **2026-08-26's citation-accuracy work found and fixed a second, more serious one** (the CrPC §122/§374 mislabeling above) rather than only documenting it — a genuine accuracy bug fixed, not just spot-checked.
- [x] Hallucination adversarial testing — **implemented 2026-08-26**: `tests/test_adversarial.py::test_known_gap_fabricated_section_embedded_in_prose_is_not_caught` — an adversarial mocked-LLM response embeds a fabricated "BNS Section 999" directly in generated document prose (outside the `citations_used` list that binding actually checks) and the test asserts it survives untouched in the response. This intentionally proves a real gap exists (citation *binding* only checks the self-reported list, never the prose itself) rather than claiming coverage that isn't there — consistent with `CLAUDE.md` §16/§21's "surface uncertainty, don't hide it" principle.
- [x] Adversarial testing (attempts to induce overconfident/unsupported claims) — **implemented 2026-08-26**: `tests/test_adversarial.py::test_case_strength_band_is_not_well_documented_when_a_contradiction_is_unresolved` — constructs a case that satisfies both of `_aggregate_band()`'s other "well-documented" conditions (fully evidence-supported claim, single hypothesis) and proves an unresolved contradiction alone is still enough to hold the band back, through the real interview/contradiction-flagging API path (not a direct DB write). Confirms §15.1's naive-scoring rejection holds under an adversarial setup, not just the happy path already covered by `test_case_strength.py`.
- [~] Agent-level evaluation (each agent tested against labeled scenarios) — **partially covered as a side effect of building each agent**: every agent has mocked-LLM tests proving its deterministic logic, gating, fabrication guards, and short-circuit behavior (76 tests total across the suite) — but none of this evaluates whether the *actual LLM output quality* is good on realistic scenarios, since no API key is configured in this dev environment to run live-LLM tests. That gap is real and unaddressed — **genuinely blocked**, not something more test-writing can close without a working `ANTHROPIC_API_KEY`.
- [ ] End-to-end case simulations (synthetic/anonymized scenarios) — not done; **BLOCKED**, same dependency as above — would need a working API key to be meaningful (a mocked end-to-end run would just be re-testing the orchestration logic already covered elsewhere, not a real simulation).
- [ ] Legal-expert review of outputs — **BLOCKED**, unchanged: no confirmation this resource is available (`CLAUDE.md` §20). Not something achievable by continuing to work through the backlog — genuinely requires a human with legal training this project doesn't have access to.

**Phase 6 status (2026-08-26):** everything achievable without a working LLM API key or a legal-expert reviewer is now done — 5 of 8 items closed, including one real accuracy bug found and fixed (not just documented) as a direct result of writing the citation-accuracy check. The remaining 3 items (agent-level LLM-quality evaluation, end-to-end case simulations, legal-expert review) are correctly left open as genuinely blocked, not incomplete for lack of effort.

## Phase 7 — Security, Privacy, and Compliance

- [x] Authentication implementation — **DECIDED as N/A for v1** (`CLAUDE.md` §18): no auth by design, session-only. Nothing to implement; this is a decision, not a gap.
- [x] Authorization implementation — **RESOLVED 2026-08-26**: re-examined and confirmed there is nothing further to build without reversing the standing no-auth decision (`CLAUDE.md` §18, Phase 2) — "case_id possession = access" isn't a partial implementation of a real access-control layer, it *is* the full decided model for this scope; there is no separate authorization check that could be added on top of it without introducing actual user identity, which would be a scope change to the no-auth architecture itself, not a Phase 7 gap-closing task. Every endpoint already 404s cleanly on an unknown case_id (input validation, not authorization). Closed as matching the authentication line above, not left open.
- [~] Data encryption (at rest, in transit) — **scoped and partially implemented 2026-08-26**: asked the user directly rather than guessing given the real cost/risk tradeoff previously flagged in `CLAUDE.md` §18 (full SQLCipher migration = new DB driver + connection-string changes across `db.py`/`config.py`, real migration risk for a local-only dev build with no real user data). **Decision: Fernet-encrypt evidence files only, skip SQLCipher.** See the next line for what shipped. The SQLite DB itself (case/claim/statement/evidence *metadata*) remains unencrypted — a real, now explicitly-scoped (not just vaguely flagged) gap for any real-user deployment. Encryption in transit remains unimplemented (assumes a TLS-terminating reverse proxy in any real deployment, per `CLAUDE.md` §18 — still correct for this local/dev-only target).
- [x] Secure evidence storage implementation — **implemented 2026-08-26**: this line's own prior note ("moot for now, no file upload exists") had gone stale — file upload shipped in an earlier session, so real evidence content had been sitting unencrypted on disk with nothing tracking it as a live gap. New `app/encryption.py` (Fernet, `cryptography` package): a key is loaded from `EVIDENCE_ENCRYPTION_KEY` if set, otherwise auto-generated and persisted to a gitignored `data/.evidence_encryption_key` for dev-session continuity — real key management (secrets manager/KMS) stays explicitly unresolved, matching the original design note, not silently declared solved. `evidence_extraction.py` refactored to take raw bytes instead of a file path, so extraction now runs on plaintext in memory *before* encryption — a plaintext copy of an uploaded file never touches disk, not even transiently. `Orchestrator.add_evidence_with_file()` writes only the encrypted bytes (`.enc` suffix). **Verified, not just built**: `tests/test_encryption.py` uploads a real PNG through the real endpoint and reads the actual bytes written to disk — confirms the PNG magic-byte header is absent from the on-disk file and that `decrypt_bytes()` recovers the exact original plaintext.
- [ ] Privacy policy drafted — not done; correctly requires actual legal drafting, not an AI-authored substitute, per the existing project principle. Left untouched this session for the same reason — not something to fake.
- [x] Audit logging implementation — **DONE 2026-08-24**, see Phase 2/5 (`AuditLogEntry`, `GET /cases/{id}/audit-log`, `AuditLogPanel`).
- [ ] Data retention strategy implementation — **explicitly asked, and the user chose to leave this undone 2026-08-26** (options offered: hard-delete everything including audit log; hard-delete case data but retain orphaned audit log entries for compliance, which would require decoupling the cascade relationship — a real schema change baking in an answer to a question `CLAUDE.md` §18 flagged as genuinely unresolved; or skip). Stays `[ ]` as a deliberate, now-confirmed decision not to build a deletion mechanism this session, not an oversight.
- [x] Sensitive-domain safeguards implementation — **DECIDED 2026-08-24** (Phase 2, `CLAUDE.md` §18): the "safeguard" for POCSO/mandatory-reporting risk is a hard scope boundary (no real-user real-abuse-narrative use), not a technical content-detection system — deliberately, since a keyword-flagging system would itself be an unreliable, false-confidence-inducing thing to build without legal guidance on what it should even do when triggered.

**Phase 7 status (2026-08-26):** 4 of 7 items closed (authentication, authorization, secure evidence storage, sensitive-domain safeguards), 1 partially closed and explicitly scoped (encryption — evidence files done, DB encryption deliberately skipped after asking), 2 correctly left open (privacy policy — requires real legal drafting; data retention — user chose to skip after being asked). Nothing left ambiguous about what's done versus deliberately not done.

## Phase 8 — UI and Deployment

- [ ] User flows designed (interview flow, case dashboard, document review flow) — still not a designed flow, just every panel stacked vertically in one page. No visual/UX design pass done.
- [~] Frontend implementation — **caught up to the backend 2026-08-24**: `frontend/src/api.ts` (typed client, now 15 endpoints) + one panel per agent/feature (`InterviewPanel`, `TimelinePanel`, `ClaimsEvidencePanel`, `ClassificationPanel`, `DevilsAdvocatePanel`, `StrategyPanel`, `DocumentsPanel`, `CaseStrengthPanel`, `AuditLogPanel`). Legal Strategy's acknowledgment gate is a real UI element, not just a backend flag — verified live in a browser, not just type-checked. **Verified running, not just built**: started both dev servers, drove the app with a headless Chrome via Playwright (`chromium-cli` wasn't available in this environment; adapted the fallback per the `run` skill's guidance), screenshotted all 7 panels rendering with zero console errors, added a real claim end-to-end against the live backend, and exercised the narrative-submission failure path (no `ANTHROPIC_API_KEY` configured here) to confirm it fails gracefully with a visible error message rather than crashing. **Bug found and fixed via this live check**: the Legal Strategy disclaimer shown to users literally said "Re-request with acknowledged=true" — API parameter syntax leaking into user-facing copy; removed the sentence. Timeline and Audit Log panels added 2026-08-24 and **also verified live**: Timeline correctly showed "No dated events yet" on an empty case, and Audit Log correctly displayed the real `claim_added` entry (with timestamp) after adding a claim through the UI — confirming the panel reads real data, not a stub. Still open: no design pass (panels are functional, not designed), no user-flow structure (everything is one flat page), evidence upload is metadata-only (no file upload UI, matching the backend's current scope).
- [~] Backend implementation — **scaffolded 2026-08-24**: `backend/` (FastAPI), running and tested (`pytest` passes, live health-check + case-creation verified). Only the fact-extraction path is wired; most agents and endpoints from Phase 4/5 are not implemented yet.
- [~] API design and implementation — **all 13 endpoints now consumed by the frontend, not just scaffolded**: `GET /health`, `POST /cases`, `POST /cases/{id}/narrative`, `/classify`, `/claims`, `/evidence`, `/evidence-gaps`, `/devils-advocate`, `/strategy`, `/documents`, `/interview/next-question`, `/interview/answer`, `GET /cases/{id}/strength`. No auth (per §18 decision), no versioning, no error-response schema yet beyond ad hoc `{"error": ...}`.
- [ ] Deployment setup (per chosen target) — not applicable yet, local/dev-only target (§19) has no separate deployment step to set up.
- [ ] Monitoring setup — not done; would need a real deployment target to monitor.
- [x] Logging setup — **DONE 2026-08-24**: basic structured Python logging configured in `main.py` (`logging.basicConfig`), with the unhandled-exception handler below logging the full traceback server-side on every 500.
- [x] Error handling and graceful-degradation behavior — **real bug found and fixed 2026-08-24**: a global FastAPI exception handler was missing, so an unhandled exception (e.g. missing `ANTHROPIC_API_KEY`) propagated past `CORSMiddleware` and the browser reported a misleading "blocked by CORS policy" error instead of the real failure — this was originally discovered live during frontend browser testing, not found by reading code. Added `unhandled_exception_handler()`, verified two ways: a unit test (`tests/test_error_handling.py`) asserting clean JSON + correct CORS header, and a live browser re-run of the same failure path confirming the confusing CORS message is gone, replaced by a plain 500. "Retrieval-uncertain/low-confidence" graceful degradation was already covered by each agent's own `insufficient_data`/`insufficient_case_state` pattern, built alongside each agent rather than as a separate pass.

## Phase 9 — Final Validation

- [ ] Full system testing — the 33-test suite covers unit/integration logic; no full end-to-end system test (real narrative → real classification → real documents, with a working API key) has been run.
- [ ] Benchmarking against defined evaluation criteria (Phase 6 outputs) — premature; Phase 6 itself is only a first pass (see above).
- [ ] Legal expert review — **BLOCKED**, same dependency as the Phase 6 item.
- [ ] Documentation finalized — **not applicable mid-build**: `CLAUDE.md`/`COMPLETION.md` are living documents by design (per `CLAUDE.md`'s own header) and are being kept current continuously, not "finalized" as a discrete milestone while active feature work continues.
- [ ] Final demo prepared — not started; this is an end-of-project deliverable, not something to build mid-backlog-cleanup.
- [ ] Project report / retrospective written — same as above, an end-of-project deliverable.

**Honest note on this phase (2026-08-24):** Phase 9 is not something that can be "completed" by continuing to work through the open-items backlog — it depends on Phase 6 (which itself depends on a legal-expert resource this project doesn't have confirmed access to) and on the project actually being declared feature-complete by the user. Flagged here rather than silently left unaddressed.

---

## Open Items Requiring User Input (live list — mirrors tags in `CLAUDE.md`)

This section tracks every unresolved `DECISION REQUIRED` / `OPEN QUESTION` / `ASSUMPTION TO VALIDATE` currently blocking downstream phases. Updated as the discovery interview proceeds.

1. ~~Primary target persona for v1~~ — DECIDED: multi-persona (`CLAUDE.md` §3). Follow-up open: persona-specific presentation layer design.
2. ~~Jurisdiction scope for v1~~ — DECIDED: India + select state laws (`CLAUDE.md` §12.1). Follow-up open: which states, selection basis.
3. ~~Legal domain scope for v1~~ — DECIDED: broad multi-domain (`CLAUDE.md` §12.1). Follow-up open: first-wave domain list (now the lead Phase 1 item).
4. ~~Project context~~ — DECIDED: portfolio project (`CLAUDE.md` §3.1, §19).
19. ~~Scope-sequencing sign-off~~ — DECIDED 2026-08-24: narrow-first ingestion confirmed by user (`CLAUDE.md` §12.1).
20. ~~Product boundaries draft~~ — CONFIRMED 2026-08-24 as final (`CLAUDE.md` §5).
21. ~~First-wave domain~~ — DECIDED 2026-08-24: criminal law (`CLAUDE.md` §12.1). Follow-up open: expansion order after criminal law.
22. ~~State-law inclusion~~ — DECIDED 2026-08-24: central + Karnataka (`CLAUDE.md` §12.5). Follow-up open: which specific Karnataka acts.
5. Orchestration pattern for multi-agent system (`CLAUDE.md` §8).
6. ~~Automation level for Legal Strategy agent~~ — DECIDED: ranks options + suggests best path, non-binding (`CLAUDE.md` §8.5). ~~Follow-up: gate ranked recommendations behind explicit user acknowledgment?~~ — RESOLVED 2026-08-24: yes, implemented as an `acknowledged` flag gating the endpoint's output.
7. ~~Case-law/precedent feature~~ — DECIDED: deferred to later phase (`CLAUDE.md` §13.2). Briefly reopened 2026-08-24, then re-confirmed deferred same day after research showed no viable citator source.
8. ~~Evidence handling depth~~ — DECIDED: basic OCR/text extraction, no audio/video analysis (`CLAUDE.md` §11.3).
9. ~~Case strength presentation: fully disaggregated vs. some coarse aggregate indicator~~ — DECIDED 2026-08-24: both — full detail always shown, plus one deterministic (non-LLM) coarse band with an explicit disclaimer (`CLAUDE.md` §15.2).
10. ~~State-specific law inclusion~~ — DECIDED: central + Karnataka (`CLAUDE.md` §12.5); see item 22.
11. ~~Amendment/update process for the knowledge base~~ — DECIDED 2026-08-24: automated periodic re-check (`CLAUDE.md` §12.4). Follow-up open: full auto-apply vs. detection-only with human-gated application (Phase 2).
12. ~~Authoritative source selection for statutory text~~ — DECIDED 2026-08-24: e-Gazette > India Code > Karnataka DPAL > Indian Kanoon (paid API) (`CLAUDE.md` §12.2).
13. ~~Multi-language support requirement~~ — DECIDED: English only for v1 (`CLAUDE.md` §23).
14. ~~Authentication requirement~~ — DECIDED: no auth, session-only (`CLAUDE.md` §18). Data-retention policy for session data still open.
15. Sensitive-domain (mandatory-reporting-adjacent, minor-safety) handling policy (`CLAUDE.md` §18).
16. Technology stack — LLM approach (API-based) and language/framework sequencing decided; specific vector DB, datastore, backend/frontend framework, and deployment target still open, to be proposed at Phase 2 (`CLAUDE.md` §19).
17. Whether a lawyer-review handoff/marketplace is ever in scope (`CLAUDE.md` §22).
18. Legal-expert review availability for evaluation (`CLAUDE.md` §20).
23. ~~Citator-like data source availability/licensing~~ — RESOLVED 2026-08-24: not available at portfolio-project cost; precedent retrieval (tier 4) descoped as a result (`CLAUDE.md` §13.2).
24. ~~Sourcing for criminal-law document tier 2 and tier 5~~ — RESOLVED 2026-08-24: tier 2 (POCSO/NDPS/UAPA/IT Act) sources from India Code same as tier 1; tier 5 (police manuals) has no public source and is descoped (`CLAUDE.md` §12.1).

---

## Change Log

- **2026-08-21** — Tracker created alongside initial `CLAUDE.md` draft. Phase 0's first three items marked complete (idea captured, initial docs drafted). All other items pending discovery interview.
- **2026-08-21** — Discovery round 1: decided target users (multi-persona), jurisdiction (India + select states), domain scope (broad multi-domain), project context (portfolio). Scope risk flagged pending sequencing sign-off.
- **2026-08-21** — Discovery round 2: decided Legal Strategy agent automation level (ranked + suggested path), evidence handling depth (OCR/text extraction), case-law/precedent deferred to later phase.
- **2026-08-21** — Discovery round 3: decided LLM approach (API-based hosted), stack selection deferred to Phase 2 design, authentication (none, session-only for v1), language support (English only for v1).
- **2026-08-24** — Phase 0 closed out: problem statement (§2) and product boundaries (§5) confirmed as drafted; scope-sequencing risk resolved (user signed off on narrow-first ingestion, `CLAUDE.md` §12.1); per-domain legal/ethical risk enumeration deferred to Phase 1 pending first-wave domain selection. **Phase 0 complete.** Starting Phase 1.
- **2026-08-24** — Phase 1 discovery round 1: first-wave domain decided as criminal law (BNS/BNSS/BSA + historical IPC/CrPC/Evidence Act); state-law scope decided as central + Karnataka (not central-only). Required-documents sub-items resolved as a consequence. Domain risk enumeration (carried from Phase 0) now unblocked and added as a Phase 1 task.
- **2026-08-24** — Phase 1 research round 1: background agent identified 4 Karnataka criminal-law acts and compared candidate data sources. User then locked in the full v1 criminal-law document hierarchy (5 tiers, including central special laws and police manuals beyond the original core-codes scope) and **reversed the 2026-08-21 case-law/precedent deferral for criminal law specifically**. Source hierarchy and manual-download acquisition method decided. Tiers 2 (special laws) and 5 (police manuals) still need dedicated source research.
- **2026-08-24** — Phase 1 research round 2: background agent investigated citator availability (tier 4) and police-manual sourcing (tier 5). Neither is viable at portfolio-project scope — no citator exists below SCC Online/Manupatra subscription cost, and no public source for a Karnataka Police Manual/Standing Orders was found. User **descoped both tiers**; the 2026-08-21 precedent-retrieval deferral is back in force. Criminal-law v1 document scope is now a **3-tier** hierarchy (core legislation, central special laws, Karnataka state laws). `MANIFEST.md` updated accordingly.
- **2026-08-24** — Phase 1 discovery round 2: effective-date/successor-pointer data model confirmed as firm requirement (`CLAUDE.md` §12.3). Amendment-tracking strategy decided as automated periodic re-check (`CLAUDE.md` §12.4) — a deliberate departure from the doc's own manual-review-gated recommendation; risk flagged for Phase 2 to resolve (full auto-apply vs. detection-only). User began downloading Tier 1–3 documents per `MANIFEST.md` in parallel.
- **2026-08-24** — User directed a jump to code scaffolding. Remaining Phase 1 practice-of-law/privacy research items deferred (not dropped); domain risk enumeration explicitly skipped. Phase 2 orchestration pattern, agent communication model, and full tech stack decided in one pass to unblock scaffolding (see `CLAUDE.md` §8.1, §8.8, §19 and Phase 2 checklist above). **Phase 2's foundational decisions are complete; detailed design work (per-agent contracts, RAG chunking specifics, Case Builder schema, security architecture beyond auth) remains open and will be filled in as those parts are actually built.**
- **2026-08-24** — First working slice scaffolded and verified running: `backend/` (FastAPI + SQLAlchemy/SQLite, `POST /cases`, `POST /cases/{id}/narrative` wired through a hand-coded `Orchestrator` to a `FactExtractionAgent` calling the Anthropic API), `frontend/` (React + Vite + TS, minimal case-intake UI with a persistent AI-draft disclaimer per §5/§22). Backend tests pass (`pytest`); frontend type-checks and builds; end-to-end health check and case creation verified live. Case Builder data model implemented per §10.2's conceptual sketch (Case/Party/Event/Statement/Claim/Evidence/DocumentDraft) — **without versioning**, since §10.2's versioning `ASSUMPTION TO VALIDATE` is still unresolved; tracked below as a Phase 5 gap. Git repo initialized, changes staged but not committed. Only Fact Extraction is wired up — Legal Classification, Law Retrieval, Evidence Gap Analysis, Devil's Advocate, and Document Generation agents are not yet implemented (Phase 4/5 work, not done here).
- **2026-08-24** — User flagged (correctly) that RAG retrieval can't function with no documents ingested — `data/raw/criminal-law/` only has `MANIFEST.md`, nothing downloaded yet. Confirmed and addressed: added Legal Classification agent (hypotheses only, no citations — doesn't need the corpus), a Phase 3 ingestion pipeline first pass (`app/ingestion.py`: PDF/text extraction, naive section-regex chunking — explicitly flagged as not yet the real structure-aware chunker §12.6 requires, metadata tagging, ChromaDB embedding), and a Law Retrieval agent (`app/law_retrieval.py`) that queries ChromaDB directly with zero LLM involvement in the retrieval step itself, honoring §8.4's hard requirement against fabricated citations. Verified live: with the corpus empty, ingestion returns 0 chunks and retrieval returns `[]`/`insufficient_data: true` — confirmed by direct smoke test, not assumed. Both new agents wired into the orchestrator via a new `POST /cases/{id}/classify` endpoint. Tests still pass.
- **2026-08-24** — Two follow-ups: (1) chunker upgraded to extract `section_number`/`section_title` as structured metadata, not just raw split text — added 4 unit tests against realistic BNS-style sample text, all passing, including multi-digit and lettered-suffix section numbers ("103A"). Still not the real §12.6 sub-section-aware chunker. (2) Evidence Gap Analysis agent implemented — evidence-support status computed deterministically from the DB (never by the LLM, structurally enforcing §11.2), LLM used only for domain-informed evidence-type suggestions (§11.3); `partially_supported`/`disputed` statuses left unimplemented rather than faked, since the schema has no sub-claim or evidence-disputation modeling yet. Added `POST /cases/{id}/claims`, `/evidence`, `/evidence-gaps` endpoints and a mocked-LLM end-to-end test proving supported/unsupported claims are distinguished correctly. **Also fixed a bug found in the process:** the test suite was writing into the real dev SQLite file (`data/db/legal_lens.db`) instead of an isolated test database — added `tests/conftest.py` with an in-memory DB + FastAPI dependency override; confirmed the dev DB is clean again (0 rows) and stays that way across test runs.
- **2026-08-24** — `MANIFEST.md` given exact target filenames for all 14 v1 documents (matches `ingestion.py`'s filename-as-act-name convention). Devil's Advocate agent implemented (`app/agents/devils_advocate.py`): operates on the same persisted statements/events/claims as every other agent, cannot invent facts to attack (§8.6); reuses the evidence-status computation refactored out of Evidence Gap Analysis into `Orchestrator._compute_claim_statuses()` so both agents agree on which claims are unsupported. Verified to skip the LLM call entirely on an empty case (test asserts the mock raises if called) rather than generating generic filler. New endpoint `POST /cases/{id}/devils-advocate`. 9/9 tests passing; dev DB confirmed still clean.
- **2026-08-24** — User asked the assistant to download the criminal-law corpus, reversing the manual-download decision. Robots.txt was checked first (not assumed): Karnataka DPAL permits crawling; India Code was inconclusive, but only single known-document URLs were fetched, not bulk crawling — a materially different risk than the original decision was declining. All 13 available documents downloaded (Karnataka Police Complaints Authority "rules" turned out not to be a separate document — its content is §20C/20D within the Police Act itself, already captured). Every file verified to have real extractable text, not scanned images — this check caught and discarded a 56MB scanned-image IPC PDF and a POCSO "overview" summary that wasn't the actual act, both replaced with verified correct sources. 2 Karnataka acts (KCOCA, Goonda Act) came from a secondary aggregator (PRS Legislative Research) rather than DPAL directly — flagged in `MANIFEST.md` for re-verification, not silently treated as authoritative. Ingested: 5,091 chunks. **Retrieval verified live against real data for the first time**: query for "punishment for murder" correctly returned BNS §103 with full metadata. Full per-document provenance recorded in `MANIFEST.md`. Phase 3's document-acquisition and embedding-build items marked done; extraction and chunking items upgraded from "smoke-tested against nothing" to "verified against the real corpus."
- **2026-08-24** — Final two core agents implemented, closing out the §8 agent roster. Legal Strategy agent (`app/agents/legal_strategy.py`): resolved §8.5's previously-open gating question — ranked recommendations are now gated behind an explicit `acknowledged` flag, returning only a disclaimer without it; rationale required to cite only facts/evidence/provisions actually passed in. Noticed and fixed a tracker gap in the process: Legal Strategy had never been given its own Phase 4 checklist line despite being fully specced in §8.5 — added rather than silently absorbed. Complaint/Document Generation agent (`app/agents/document_generation.py`): single parameterized agent handles all 7 draft types from §8.7; watermarking enforced two ways (prompt-level required label, and `DocumentDraft.review_status` never set to anything but its `"ai_draft"` default in this code path); no fabricated facts/citations, missing details become explicit placeholders. Both agents wired into the orchestrator and exposed via `POST /cases/{id}/strategy` and `POST /cases/{id}/documents`. 14/14 tests passing (mocked LLM calls — no API key configured in this dev environment, so live-LLM smoke testing wasn't possible here, only the deterministic/gating/watermarking logic). Dev DB confirmed still clean.
- **2026-08-24** — Watermarking/AI-draft-labeling removed from Document Generation at the user's explicit instruction — both the content-level required label and its enforcing prompt instruction. `review_status` field left untouched but no longer surfaced as a warning. This directly narrows §5/§22's product-boundary and human-in-the-loop posture — flagged as a real gap in `CLAUDE.md` §8.7 and here, not silently dropped. Test updated to match (no longer asserts a watermark string); 14/14 still passing.
- **2026-08-24** — Dynamic Interview implemented: question generation, hybrid stopping criterion (resolves §9.2's open question), and contradiction flagging (§9.3). New `InterviewTurn` entity, flagged as outside §10.2's original sketch. **Bug caught and fixed via the test suite**: the orchestrator called Legal Classification unconditionally even on a case with zero facts, before the emptiness check that should have skipped it — now fixed to match the short-circuit pattern already used by other agents. 19/19 tests passing (5 new); dev DB confirmed clean. §9.4 (jurisdiction detection) remains open, untouched by this work.
- **2026-08-24** — Case Strength analysis implemented, closing out Phase 4 (all core-intelligence pieces now have a first pass). §15.2's aggregate-indicator question resolved: one deterministic coarse band alongside full disaggregated detail. Verified — not just documented — that §15.1's naive-scoring rejection actually holds: a test walks the full response tree checking for forbidden score-shaped keys. Same bug class as Dynamic Interview's found again and fixed at the shared source (`classify_and_retrieve()`), which three callers now benefit from together. 23/23 tests passing; dev DB confirmed clean.
- **2026-08-24** — Frontend built out to match the backend: typed API client + 7 new panel components covering all agents built since the original scaffold, including a real acknowledgment-gate UI for Legal Strategy. **Actually run in a browser, not just built**: dev servers launched, driven with headless Chrome (Playwright) since `chromium-cli` wasn't available — screenshots confirm all panels render with zero console errors; live case creation, claim creation, and the Legal Strategy gate all verified working against the real backend; narrative-submission failure (no API key here) confirmed to degrade gracefully. **Found and fixed a real bug via this check**: the Legal Strategy disclaimer exposed raw API syntax ("acknowledged=true") to end users — visible only in the actual screenshot, not in code review. Dev DB (polluted by live smoke-test cases) reset to clean afterward; 23/23 tests still passing.
- **2026-08-24** — User asked to work through the entire remaining open-items backlog. Large batch of real progress, summarized by phase:
  - **Phase 1**: closed out all 3 remaining research items (domain risk enumeration, practice-of-law constraints, DPDP privacy requirements) via a dedicated research agent — found two previously-untracked risks (POCSO §19 personal mandatory-reporting exposure, Section 228A IPC/BNS §72 victim-identity disclosure) now incorporated into `CLAUDE.md` §18/§21.
  - **Phase 2**: versioning strategy decided (`AuditLogEntry`, doubling as audit logging), citation-binding mechanism decided and implemented, retrieval-confidence handling confirmed already covered, per-agent contracts decided to be the code itself, security architecture decided across the board (auth/authz/encryption/retention/sensitive-domain policy) — several explicitly decided as **not implemented**, flagged as real gaps rather than checked off.
  - **Phase 3**: citation binding implemented for Legal Strategy (not yet Document Generation).
  - **Phase 4/5**: Timeline construction implemented and verified live; Question & Answer Preparation (§14) implemented as a new agent, closing both the "question preparation" and "response preparation" checklist items; Case Builder versioning resolved via the same `AuditLogEntry` mechanism.
  - **Phase 6**: first-pass retrieval evaluation against the real corpus (5 hand-verified gold queries, including one that proves §12.3's dual-code retrieval works) — and, honestly, a **discovered weakness** (POCSO/NDPS retrieval quality) documented rather than hidden. Legal-expert review remains correctly blocked.
  - **Phase 7**: audit logging implemented; encryption/retention/authz decided (several as explicit non-implementation, not silent gaps); sensitive-domain safeguards decided as a hard scope boundary rather than an unreliable keyword-detection system.
  - **Phase 8**: logging setup added; a **real bug found via live browser testing** (unhandled exceptions bypassing CORS headers, producing a misleading browser error) found and fixed, verified both by unit test and a live re-run of the original failing scenario.
  - **Phase 9**: handled honestly — not pretended complete. Legal-expert review, benchmarking, final demo, and project report are correctly treated as blocked/not-yet-applicable rather than silently skipped.
  - Test suite grew from 23 to 36 tests across this batch, all passing; dev DB confirmed clean throughout via repeated resets between live-testing rounds.
- **2026-08-24** — User asked to complete everything remaining through Phase 4. Phases 0-4 now fully closed except one deliberate exclusion (similar-case retrieval, correctly still deferred). Highlights: caught and fixed a real security incident (user's live API key was pasted into the tracked `.env.example` instead of gitignored `.env` — fixed before any commit happened); jurisdiction detection and inter-agent conflict resolution both went from "genuinely open" to designed-and-implemented-and-tested; real evidence file upload + OCR built and verified with actual tesseract runs, not mocks; document checksums + change-detection re-ingestion implemented and verified live against the real 13-document corpus (processed all 13, then correctly skipped all 13 on a repeat run); gold-standard retrieval eval set grew from 5 to 14 real, hand-verified queries. Test suite: 36 → 58, all passing.
- **2026-08-25** — User asked to close out everything genuinely still open through Phase 4. Re-audited the tracker against the actual code first (not just re-reading prior claims) and found several `[~]` lines were either stale (already resolved elsewhere but not flipped here) or named a real, scoped gap rather than something requiring Phase 6's blocked legal-expert resource. Fixed the real gaps, not just the bookkeeping:
  - **Effective-date metadata (`CLAUDE.md` §12.3)**: `app/ingestion.py` gained `ACT_EFFECTIVE_DATES`, a per-act `effective_from`/`effective_to`/`repealed_by`/`successor_of` table (best-effort from public enactment dates, not re-verified against e-Gazette per document — consistent with §12.2's acquisition policy), threaded through chunk metadata and `Orchestrator._flatten_retrieved_provisions()`. Corpus re-ingested with `force=True` (still 5,091 chunks) and **verified live**: BNS §103 now returns `effective_from: 2024-07-01, successor_of: IPC_1860`; a "criminal intimidation" query returns both BNS §351 and its historical IPC §503/506/507 counterparts correctly tagged `effective_to: 2024-06-30, repealed_by: BNS_2023` — §12.3's dual-code data model is now actually queryable end-to-end, not just designed.
  - **Fact Extraction JSON retry/repair**: new `Agent._call_model_json()` helper (`app/agents/base.py`) retries once with a repair instruction (original request + the invalid output) before falling back to the existing `parse_error` shape. Applied to `FactExtractionAgent`, the one explicitly flagged as missing this. Verified by two tests: recovery after one bad response, and the fallback still firing after both attempts fail.
  - **`partially_supported`/`disputed` claim statuses (`CLAUDE.md` §11.3)**: previously left uncomputed pending "sub-claim and evidence-disputation modeling this schema doesn't have yet" — built that modeling rather than continuing to defer it. New `Evidence.disputed` boolean (never auto-inferred, only set via new `PATCH /cases/{id}/evidence/{evidence_id}/dispute`) and extraction-confidence now both feed `Orchestrator._compute_claim_statuses()`: disputed evidence → `disputed`; undisputed but low/no-confidence extraction → `partially_supported`; otherwise → `supported`/`unsupported` as before. All four statuses now reachable and tested (`tests/test_evidence_dispute.py`).
  - **Citation binding extended to Document Generation (`CLAUDE.md` §12.6)**: previously implemented for Legal Strategy only. `DocumentGenerationAgent` now self-reports `citations_used` (which retrieved chunk_ids it actually cited); `Orchestrator.generate_document()` checks them against real retrieved provisions the same way Legal Strategy's binding does, surfacing anything unverified as `unverified_citations_dropped` + a `citation_warning` rather than trusting it. Explicitly documented limitation, not hidden: this checks the self-reported citation *list*, not whether a fabricated section number actually appears in the drafted prose — that would need sentence-level entailment checking against source text (§12.7), still not built anywhere, and correctly left to Phase 6.
  - Cleared several stale `[~]` tracker lines that were actually already complete (Legal Strategy, Devil's Advocate, Case Strength, Law Retrieval, Dynamic Interview) — one genuinely stale claim corrected in the process: Dynamic Interview's line still said jurisdiction detection was "genuinely unaddressed," but it had in fact been implemented later the same day it was written (2026-08-24), just never flagged back to update that line.
  - Dev SQLite DB deleted and allowed to regenerate (it was empty, and the new `Evidence.disputed` column needed a fresh `create_all` — no migration tooling exists in this project, consistent with its local/dev-only scope). Test suite: 58 → 67, all passing; dev DB confirmed clean (0 cases) after the run.
  - **Left deliberately untouched, and why**: (1) re-ingestion *scheduling* (the cron/background-job half of §12.4's "automated periodic re-check") — still requires infrastructure this local/dev build doesn't have; building a fake scheduler for a single-process dev server wouldn't be a real improvement. (2) Validating documents against the live authoritative source text — would require automating a fetch against e-Gazette/India Code, which §12.2's acquisition policy deliberately avoids doing without a human step. (3) Evaluating Fact Extraction/Legal Classification against labeled scenarios, and sentence-level citation-entailment checking — both correctly Phase 6 work, most of which depends on either a working API key for live-LLM runs or the still-blocked legal-expert review resource, neither newly available this session.
- **2026-08-25 (same day, follow-up)** — User asked to explicitly close out Phase 4. Re-checked what was left after the prior pass: only two lines remained `[~]`, both Fact Extraction and Legal Classification, both held back solely by an "unevaluated against labeled scenarios" caveat — which is actually Phase 6's own checklist item ("Agent-level evaluation"), not a Phase 4 implementation gap. Rather than just relabeling, did one more real piece of work first: extended the JSON retry/repair helper (built for Fact Extraction in the prior pass) to Legal Classification too, the other agent using a bare `json.loads`/`except` with no recovery — verified by two new tests (recovery after one bad response, fallback still correct after both attempts fail). Then corrected the bookkeeping: both lines flipped to `[x]` with a note distinguishing "implementation" (this phase's scope, done) from "evaluation" (Phase 6's scope, still open and still blocked). Added a closing summary line: **Phase 4 is now closed** except for similar-case/precedent retrieval, which stays `[ ]` as a deliberate 2026-08-21/08-24 scope exclusion, not an unfinished task. Test suite: 67 → 69, all passing.
- **2026-08-26** — User asked to close out Phase 5. Found three items: two real gaps and one that needed a direct decision rather than silent action.
  - **Evidence organization UI/logic** (previously `[ ]`, no code at all): added `GET /cases/{id}/evidence` (`Orchestrator.list_evidence()`) returning the full per-case evidence inventory — type, description, claim linkage, extraction confidence, disputed flag, whether a file is attached. `ClaimsEvidencePanel` gained an "Organize evidence" view (grouped by linked claim, including an explicit "not linked to a claim" group) and a working dispute toggle wired to the prior session's `PATCH .../dispute` endpoint. **Verified live in a browser**: both dev servers started, a claim and linked evidence created through the real UI (Playwright driving real Chromium, `npx playwright install chromium` — this environment had neither `chromium-cli` nor a pre-installed browser), "Organize evidence" confirmed to render the grouped inventory, the dispute toggle confirmed to flip the item to `DISPUTED`/back — zero console errors, screenshot inspected directly. Only the narrative-submission call was mocked (no working API key here, and that call gates whether the panel renders at all) — claim/evidence/dispute themselves hit the real backend. Dev DB reset to clean afterward (it had picked up 4 real cases from the live run).
  - **Document Generation's citation binding** (stale tracker note): the line still said "first pass" with no mention of citation binding, even though that was added in the prior session. Corrected to `[x]` and extended the frontend: `DocumentsPanel` now surfaces `citation_warning`/`unverified_citations_dropped`/`verified_citations` instead of silently discarding fields the backend already returns.
  - **AI-draft watermarking** (the one item still `[ ]`): this was explicitly removed at the user's instruction in a prior session as a deliberate product-boundary decision (`CLAUDE.md` §8.7), not an oversight — restoring it unilaterally to make the checklist look complete would reverse that decision without authorization. Asked directly instead of guessing: user confirmed **leave it removed**. Stays `[ ]`, now twice-confirmed as a deliberate exclusion rather than a gap, same pattern as Phase 4's precedent-retrieval line.
  - **Phase 5 is now closed** with that one item as its deliberate exclusion. Test suite: 69 → 71, all passing.
- **2026-08-26 (same day, follow-up)** — User asked to verify Phases 0-5 were genuinely done before starting Phase 6. Audited every remaining `[~]`/`[ ]` line in Phases 0-5 against the actual code rather than trusting prior notes; confirmed every one is either a decided/deliberate exclusion (precedent retrieval, AI-draft watermarking) or an honestly-flagged limitation correctly deferred to a later phase (re-ingestion scheduling, live-source validation, security implementation) — nothing silently incomplete. Proceeded to Phase 6:
  - **Retrieval evaluation**: corrected a stale count (this file said "5 queries," the suite actually had 14 since 2026-08-24 — fixed the note, not the count).
  - **Citation accuracy testing**: built `tests/test_citation_accuracy.py`'s self-consistency check (does every one of the real corpus's 5,136 chunks' own text actually match its own section_number label) — a real automated check, distinct from citation *binding* (chunk_id membership) and short of full semantic entailment (still correctly out of reach without a working API key). **This test caught a real bug on its first run**: a stray "Section 122." cross-reference in `CrPC_1973.pdf` was, due to the header regex's `\s*` spanning newlines, absorbing the real "374. Appeals from convictions" section header into its own mislabeled chunk. Fixed the regex (`\s*` → `[ \t]*`), added a regression test reproducing the exact shape, and re-ingested the real corpus (5,091 → 5,136 chunks — the fix helped elsewhere in the corpus too, not just the one case found first).
  - **Hallucination adversarial testing**: `tests/test_adversarial.py` adds a test that deliberately proves citation binding does NOT catch a fabricated section number embedded directly in generated prose (only the separate `citations_used` list is checked) — documents a real gap honestly rather than only testing what already passes.
  - **Overconfidence adversarial testing**: a second `test_adversarial.py` test proves Case Strength's aggregate band can't be pushed to "well-documented" by an unresolved contradiction alone, even when every other condition for that band is otherwise satisfied — through the real interview/contradiction API path, not a direct DB write.
  - **Left correctly blocked, not faked**: agent-level LLM-quality evaluation, end-to-end case simulations, and legal-expert review all still require either a working `ANTHROPIC_API_KEY` or a human legal reviewer, neither newly available this session. Phase 6 status line added summarizing 5 of 8 items closed with the rest genuinely blocked. Test suite: 71 → 76, all passing; dev DB confirmed clean.
- **2026-08-26 (same day, follow-up)** — User asked to close out Phase 7. Two items involved real security/architecture tradeoffs `CLAUDE.md` §18 had explicitly flagged as unresolved — asked the user directly rather than guessing:
  - **Encryption scope**: offered Fernet-encrypt-evidence-files-only (matches the original §18 design exactly, scoped, no DB driver change) vs. full SQLCipher-plus-Fernet vs. skip entirely. **User chose evidence-files-only.** Implemented: new `app/encryption.py` (Fernet via the `cryptography` package, newly added as a dependency), key auto-generated and persisted to a gitignored `data/.evidence_encryption_key` for dev-session continuity if `EVIDENCE_ENCRYPTION_KEY` isn't set — real key management stays explicitly unresolved, not silently declared solved. Refactored `evidence_extraction.py` to take raw bytes instead of a file path so extraction happens on plaintext in memory before encryption — no plaintext copy ever touches disk, not even transiently. **Verified real, not just written**: `tests/test_encryption.py` uploads a real PNG through the real endpoint and reads back the actual on-disk bytes, confirming the PNG header is gone and `decrypt_bytes()` recovers the exact original. This also closed the stale "Secure evidence storage — moot for now, no file upload exists" line, which had gone stale since file upload shipped in an earlier session (real evidence content had been sitting unencrypted on disk with nothing tracking it as a live gap).
  - **Data retention**: offered hard-delete-everything vs. hard-delete-but-preserve-audit-log (requires a cascade schema change, baking in an answer to a genuinely unresolved compliance-vs-completeness tension) vs. skip. **User chose skip.** Left `[ ]`, now a confirmed decision rather than an open question.
  - **Authorization**: re-examined and resolved without needing to ask — "case_id possession = access" isn't a partial implementation of a real access-control layer, it already *is* the full decided model for the no-auth architecture; nothing further could be built without reversing that architecture decision, which would be new scope, not gap-closing. Closed to match the authentication line.
  - Test suite: 76 → 79, all passing. Phase 7 status line added: 4 of 7 items now closed, 1 partially closed and explicitly scoped, 2 correctly left open by deliberate decision (privacy policy still correctly requires real legal drafting, untouched).
